# Epics — SQ-B (deterministic core): V4B1 … V4B5

PRD: `delivery/v4/prds/prd-b-core.md` — read D-B1 … D-B15 before implementing; every task below cites its D-B number.
Law: `delivery/v4/CONTRACT.md` v1.0-draft. Base branch: `main`. Worktrees-only (G-10). Status vocabulary: NOT-STARTED · IN-PROGRESS · IMPLEMENTED-NOT-VERIFIED · VERIFIED (case ID + run-output ref required). All five epics are **NOT-STARTED** (planning-only artifact; no code written).
Legend: G-n governance rule · `T-<EID>-NN` test case · Wn wave · Rn default · Dn ruling — see GLOSSARY.md
Anchors: `gate-check.mjs:<line>` = `plugins/plan-it/skills/plan-it/scripts/gate-check.mjs` @ 7fcff27 (root copy byte-identical); `guard:<line>` = `plugins/plan-it/scripts/hooks/planit-guard.mjs` (134 lines, identical to root since the W0 mirror fix). Acronyms as in the PRD's first-use block (DoD = `Definition of Done`; INV = IMPLEMENTED-NOT-VERIFIED; FD-1/FD-2 founder mandates; AMD-n amendment; LG-n live-grounding fact; F-Bn Stream B finding).
Headings use `## Epic V4B<n> —` so the 3.0.1 lint grammars (`[A-Z]\d+`) recognise them until V4B3 widens the grammar (PRD §9 item 6).
Case polarity in every `run:` cell: a `gate-check.mjs <verb> …` invocation names a violating fixture and must exit non-zero; a `node tests/v4/core/<file>.mjs` script is positive and must exit 0 (its body asserts the exact codes and messages listed in the case).

---

## Epic V4B1 — `machine.json` additive superset + byte-pinned 3.0.1 baseline + negative machine fixtures

Branch: `epic/v4b-machine-superset` (worktree off `main`)
Depends on: W0 byte-pin (verified, created only if absent — D-B2). First epic of the lane; V4B2–V4B5 build on it.
Covers: C-E7-01, C-E7-02, C-E11-04 (+ supplies the gate shape C-E11-05 asserts in V4B5)
Scope: the 25-state machine from PRD D-B1 — eight new states, three new guards mapped to `state`, mode-tagged events, three retargets through new states, context/meta additions, version 4.0.0 — proven additive against both pins, with six negative fixtures that fail `machine-diff` by name.

### Task checklist
- [ ] `tests/fixtures/v3/machine.v3.7fcff27.json` — verify SHA-256 `05d2147be8c2503a72e781da5bd1fdfe280c77dde1c2f7408ecc18e1e1d074eb` == `git show 7fcff27:plugins/plan-it/skills/plan-it/machine.json`; create only if absent; on mismatch fail closed and report (never overwrite). README beside it names T-V4B1-02/04. [D-B2]
- [ ] `machine.json` (plugin copy first, then root mirror) — `version: "4.0.0"`; `context` gains `mode: null`, `deliveryRoot: null`, `topology: null`; `meta.stateFile: ".plan-it/<slug>.state.json"`, `meta.stateFileFallback: ".plan-it/state.json"`. [D-B1]
- [ ] `machine.json` `meta.guards` — add `triageRecorded`, `defaultsRecorded`, `planReviewed`, each `check: "state"`, `usage: "node scripts/gate-check.mjs state --dir . --run <slug>"`, one-line `rule`. No existing guard touched. [D-B1]
- [ ] `machine.json` `states` — insert `triage` (phase 0; `on`: `TRIAGE_PLAN → anamnesis` guard `triageRecorded`; `TRIAGE_BUILD_INSTEAD | TRIAGE_SKIP | TRIAGE_OWNER_DECISION → closedWithoutPlan` guard `triageRecorded`), `closedWithoutPlan` (`type: "final"`, `meta.title: "CLOSED_WITHOUT_PLAN"`, `meta.does`: memo + backlog/decision card written, no package), `anamnesis` (phase 0, `gate: "G0"`, `human: true`; `G0_ANSWERED → dodLock` guard `gateRecorded`), `scopeBrief` (phase 2; `BRIEF_RENDERED → scopeGate` guard `artifactsOnDisk`), `defaultsApplied` (phase 7; `DEFAULTS_APPLIED → coherencePass` guard `defaultsRecorded`), `render` (phase 10; `RENDERED → handoff` guard `artifactsOnDisk`, `meta.mode: "guided"`; `REVIEW_READY → planReview` guard `artifactsOnDisk`, `meta.mode: "autonomous-draft"`), `planReview` (phase 10, `gate: "G4"`, `human: true`, `records: ["G2","G3"]`; `REVIEW_ANSWERED → freeze` guard `planReviewed`), `freeze` (phase 10; `CONTRACT_FINAL → handoff` guard `contractFrozen`; `REVIEW_CONTRADICTED → parallelPlanning` guard `contractFrozen`, `meta.recovery: true`). [D-B1]
- [ ] `machine.json` retargets — `intake.INTAKE_CAPTURED.target: "triage"`, `dodLock.DOD_LOCKED.target: "scopeBrief"`, `adversaryGate.ADVERSARY_CLEAN.target: "render"` (guards unchanged: none, none, `adversarialDepth`). [D-B1]
- [ ] `machine.json` new events on baseline states — `specAuthoring.SPECS_DRAFTED_AUTONOMOUS → defaultsApplied` (`meta.mode: "autonomous-draft"`), `coherencePass.COHERENT_AUTONOMOUS → backboneFreeze` (`meta.mode: "autonomous-draft"`); tag the guided-only baseline events `specAuthoring.SPECS_DRAFTED` and `coherencePass.COHERENT` with `meta.mode: "guided"` (meta on a transition is not inspected by `checkMachineAdditive`, F-B3 rule list). [D-B1]
- [ ] `machine.json` `backboneFreeze.meta.does` — mention both `freeze` (guided, v1.0) and `freeze --draft` (autonomous, v1.0-draft); `handoff.meta.does` — mention embedded glossary + mirror checks. Prose only. [D-B1]
- [ ] Negative fixtures `tests/fixtures/v4/machine/` (each a full machine JSON derived from the 4.0.0 file + README naming T-V4B1-07…12): `neg-initial-changed.json`, `neg-state-dropped.json` (drops `adversaryGate`), `neg-event-dropped.json` (drops `dodLock.DOD_LOCKED`), `neg-guard-swapped.json` (`scopeGate.G1_APPROVED` guard → `artifactsOnDisk`), `neg-retarget-baseline.json` (`freezeGate.G3_APPROVED → parallelPlanning`), `neg-guard-removed.json` (drops `meta.guards.adversarialDepth`). [D-B1, D-B2]
- [ ] Guided fixture `tests/fixtures/v4/machine/guided-run/.plan-it/g1.state.json` — `run.mode: "guided"`, history through `freezeGate` with G1/G2/G3 recorded exactly as 3.0.1 (`approved+owner+date`), `machineVersion: "4.0.0"`. [D-B1]
- [ ] `tests/v4/core/machine-additive.mjs`, `machine-negatives.mjs`, `machine-pin.mjs`, `guided-unchanged.mjs` — new scripts (positive harness; each asserts exit codes and the named reason strings from `checkMachineAdditive`, `gate-check.mjs:278–310`). [D-B1, D-B2]
- [ ] Mirror sync: `cp plugins/plan-it/skills/plan-it/machine.json machine.json`; `gate-check mirror-check` exit 0 (8/8) before the merge request.

### Tier Table

| tier | effort | escalation | scaffold-pointer |
|---|---|---|---|
| top | statechart-semantics-review (judgment slice per RUN-POLICY) | already top; a failed slice is re-run by the coordinator, recorded in the run report | build-it:iteration-impl#slice=V4B1 |
| mid | spec'd-implementation vs this Test Contract (machine edits, harness scripts) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B1 |
| low | mechanical (six negative fixtures, README, mirror copy) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B1-fixtures |

Cascade map (D4 adversary gate): partial-failure — N/A for this epic: `machine-diff` is a pure read of two files, single-target and atomic (waiver: `partial-failure: N/A — single read, no multi-target write`); rollback/compensation — N/A: nothing is written (waiver: `rollback/compensation: N/A — read-only check`); failed-recovery→escalation — N/A here (waiver: `failed-recovery→escalation: N/A — read-only check; escalation is asserted in V4B2/V4B4`); recovery/resume — T-V4B1-06 (a guided run resumes and `state` re-verifies its position); adversarial-verify — T-V4B1-04 (the pin is re-read and re-hashed against git, not trusted).

### Test Contract — V4B1 machine superset   (BINDING: 100% pass or /iterate)
Types: [unit][integration] · Count: 14 · [REAL]: 0 · Surfaces: gate-check exit codes + node harness scripts, offline
DoD = every case below PASS (T-V4B1-01 … T-V4B1-14). No [REAL] case is VERIFIED on a mock.

| ID | @tag | Case (given / when / then → expected) | run: | fixture | Covers |
|---|---|---|---|---|---|
| T-V4B1-01 | @case-machine | Given the 4.0.0 `machine.json`; when parsed; then `version === "4.0.0"`, `initial === "intake"`, exactly 25 states, `closedWithoutPlan` and `done` are the only `type: "final"` states, every transition target exists (T-E1-04 logic) | `node tests/v4/core/machine-additive.mjs` | live `machine.json` | C-E7-01 |
| T-V4B1-02 | @case-machine | Given the live machine and the 3.0.1 pin; when `gate-check machine-diff machine.json tests/fixtures/v3/machine.v3.7fcff27.json`; then exit 0 and stdout `PASS — machine additive-only vs baseline` (G-1) | `node tests/v4/core/machine-additive.mjs` | `tests/fixtures/v3/machine.v3.7fcff27.json` | C-E7-01 |
| T-V4B1-03 | @case-machine | Given the live machine and the v2 pin; when `machine-diff` against `tests/fixtures/v2/machine.v2.fc6abc8.json`; then exit 0 (G-1; the v2 chain of AMD-1 still holds through 4.0.0) | `node tests/v4/core/machine-additive.mjs` | `tests/fixtures/v2/machine.v2.fc6abc8.json` | C-E7-01 |
| T-V4B1-04 | @case-machine | Given the pin file; when its SHA-256 is recomputed from bytes and compared with `git show 7fcff27:plugins/plan-it/skills/plan-it/machine.json`; then both equal `05d2147b…074eb` (adversarial-verify: the pin is re-read, not trusted); a byte-flipped copy in a temp dir fails the same check naming both hashes | `node tests/v4/core/machine-pin.mjs` | `tests/fixtures/v3/machine.v3.7fcff27.json` | C-E7-01 |
| T-V4B1-05 | @case-machine | Given the pin and the live machine; when every baseline state, event and guard is looked up in the live file; then all 17 states, 17 events and 5 guards are present with identical guard names, and the only differing targets are exactly `intake.INTAKE_CAPTURED → triage`, `dodLock.DOD_LOCKED → scopeBrief`, `adversaryGate.ADVERSARY_CLEAN → render`, each onto a state absent from the pin | `node tests/v4/core/guided-unchanged.mjs` | `tests/fixtures/v3/machine.v3.7fcff27.json` | C-E11-04 |
| T-V4B1-06 | @case-machine | Given the guided fixture run (G1/G2/G3 recorded as 3.0.1 does, `run.mode: "guided"`); when `gate-check state --dir tests/fixtures/v4/machine/guided-run --run g1`; then exit 0, `state: freezeGate`, next events list `G3_APPROVED` only (no autonomous event printed) — a guided run resumes and re-verifies its position exactly as before | `node tests/v4/core/guided-unchanged.mjs` | `tests/fixtures/v4/machine/guided-run/` | C-E11-04 |
| T-V4B1-07 | @case-machine | Given `neg-initial-changed.json` (`initial: "triage"`); when `machine-diff` vs the 3.0.1 pin; then exit 1 and stderr contains `initial changed: "intake" → "triage"` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-initial-changed.json` | C-E7-02 |
| T-V4B1-08 | @case-machine | Given `neg-state-dropped.json` (no `adversaryGate`); when `machine-diff`; then exit 1 and `baseline state missing/renamed in live machine: "adversaryGate"` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-state-dropped.json` | C-E7-02 |
| T-V4B1-09 | @case-machine | Given `neg-event-dropped.json` (no `dodLock.DOD_LOCKED`); when `machine-diff`; then exit 1 and `baseline transition dropped: dodLock.on.DOD_LOCKED` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-event-dropped.json` | C-E7-02 |
| T-V4B1-10 | @case-machine | Given `neg-guard-swapped.json` (`scopeGate.G1_APPROVED` guard `artifactsOnDisk`); when `machine-diff`; then exit 1 and `guard mapping changed on scopeGate.on.G1_APPROVED: "gateRecorded" → "artifactsOnDisk"` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-guard-swapped.json` | C-E7-02 |
| T-V4B1-11 | @case-machine | Given `neg-retarget-baseline.json` (`freezeGate.G3_APPROVED → parallelPlanning`); when `machine-diff`; then exit 1 and the reason contains `onto a pre-existing baseline state — only insertion of NEW states is additive` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-retarget-baseline.json` | C-E7-02 |
| T-V4B1-12 | @case-machine | Given `neg-guard-removed.json` (no `meta.guards.adversarialDepth`); when `machine-diff`; then exit 1 and `baseline guard missing/renamed in live machine: "adversarialDepth"` | `node tests/v4/core/machine-negatives.mjs` | `tests/fixtures/v4/machine/neg-guard-removed.json` | C-E7-02 |
| T-V4B1-13 | @case-machine | Given the live machine; when every state with `meta.gate` is collected; then the sorted gate ids are exactly `G0, G1, G2, G3, G4`, every one has `meta.human === true`, `planReview.meta.records` equals `["G2","G3"]`, and the three new guards map to `check: "state"` so the closed set of T-E1-03 (`verify, freeze, handoff, state, adversary`) is unchanged | `node tests/v4/core/machine-additive.mjs` | live `machine.json` | C-E11-04 |
| T-V4B1-14 | @case-machine | Given the live machine; when mode tags are read; then `SPECS_DRAFTED_AUTONOMOUS`, `COHERENT_AUTONOMOUS`, `render.REVIEW_READY` carry `meta.mode: "autonomous-draft"`, `SPECS_DRAFTED`, `COHERENT`, `render.RENDERED` carry `"guided"`, every other transition is untagged, `closedWithoutPlan.meta.title === "CLOSED_WITHOUT_PLAN"` and `freeze.REVIEW_CONTRADICTED` targets `parallelPlanning` (the recovery edge exists in the machine) | `node tests/v4/core/machine-additive.mjs` | live `machine.json` | C-E7-01 |

---

## Epic V4B2 — `state` verb additions (triage · defaults · plan review · draft-cannot-hand-off · mode consistency · `--run`) + `freeze --draft`

Branch: `epic/v4b-state-draft` (worktree off `main`)
Depends on: V4B1 (the machine the verb walks)
Covers: C-E6-01, C-E6-02, C-E7-03, C-E7-04, C-E7-05, C-E7-06, C-E7-07, C-E9-01
Scope: the seven additive `state` checks of PRD D-B3 and the draft freeze of D-B4. Every REJECT names its fix and its CONTRACT case ID (D-B15). Nothing fires on a state file that lacks the new keys — every v2/v3 fixture keeps passing byte-for-byte.

### Task checklist
- [ ] `gate-check.mjs:934–940` (`cmdState` arg parsing) — add `parseRunFlag(rest)` (sibling of `parseDirFlag`, `:1080`) so `state --dir <root> --run <slug>` reads `<root>/.plan-it/<slug>.state.json` (missing ⇒ `fail` naming the path, C-E9-01). Positional form unchanged. [D-B3 (7), D-B5]
- [ ] `gate-check.mjs:960` — root-derivation regex → `/[\\/]\.plan-it[\\/](?:[a-z0-9][a-z0-9-]*\.)?state\.json$/` so `state <root>/.plan-it/v4.state.json` derives the root (no longer "bare", F-B9). [D-B3]
- [ ] `gate-check.mjs` after `:978` — **check 1 triage** (`history` contains `triage` ⇒ `triage.verdict ∈ {plan, build-instead, owner-decision, skip}`, `triage.measuredAt` matches `\d{4}-\d{2}-\d{2}`, `triage.measurements` is an array; non-plan verdict ⇒ `<root>/<triage.memo>` exists; messages carry `C-E6-01` and read `CLOSED_WITHOUT_PLAN REJECTED: verdict "<v>" requires triage.memo on disk`). [D-B3 (1)]
- [ ] after `:978` — **check 2 defaults** (`defaultsApplied` visited ⇒ `gates.G2.defaults` non-empty array, every row `{id, source:"recommended", contradicted:boolean}`; message carries `C-E7-07` and the offending row id). [D-B3 (2)]
- [ ] after `:978` — **check 3 plan review** (`gates.G4.approved === true` ⇒ `<root>/<run.deliveryRoot ?? "delivery/">PLAN-REVIEW.md` exists + `/^Reviewed-by:\s+\S.*\b\d{4}-\d{2}-\d{2}\b/m`; `gates.G2`/`gates.G3` approved+owner+date via `planReview.meta.records`; every default `contradicted:false` or matched by `id` in `gates.G4.contradictions[]`; messages carry `C-E7-03`). [D-B3 (3)]
- [ ] after `:978` — **check 4 draft cannot hand off** (`state ∈ {handoff, done}` ∧ `/-draft$/.test(contract.version)` ⇒ `fail("C-E7-04 (G-13): draft contract cannot hand off — contract.version \"" + v + "\" …")`). [D-B3 (4)]
- [ ] `:969–978` — **check 5 mode-aware FD-2 artifact**: guided ⇒ `<root>/<deliveryRoot ?? "delivery/">TEST-CONTRACT-REVIEW.md` (byte-identical behaviour when `deliveryRoot` is unset); autonomous-draft ⇒ B1/B2 read `PLAN-REVIEW.md`; `gates.G2.approved:true` before `planReview` accepted only with `gates.G2.pendingReview === true`, else REJECTED (`C-E7-06`). [D-B3 (5)]
- [ ] after `:978` — **check 6 mode consistency** (`run.mode` autonomous-draft ∧ visited ∩ {decisionGate, freezeGate} ≠ ∅ ⇒ fail; guided ∧ visited ∩ {defaultsApplied, planReview, freeze} ≠ ∅ ⇒ fail; `history` entry `{state:"freeze", event:"REVIEW_CONTRADICTED"}` accepted; a default id appearing twice in `gates.G4.contradictions[]` must carry `escalated:true` and `card` (path exists) ⇒ else `fail("C-E7-06: default <id> contradicted twice without ESCALATED card — never re-default")`). [D-B3 (6)]
- [ ] `:1059–1072` — **printout filter**: skip transitions whose `meta.mode` is set and ≠ `run.mode`; final states still print `next events: (final state)`. [D-B3 (7)]
- [ ] `gate-check.mjs:79–98` (`cmdFreeze` parse) — accept `--draft` and `--run <slug>`; `:107` regex → `/\bv\d+\.\d+(?:-draft)?\b/`; new rule: `--draft` ∧ header lacks `-draft` ⇒ fail; no `--draft` ∧ header has `-draft` ⇒ `fail("C-E7-05 (G-13): draft cannot be final — header v1.0-draft; run freeze --draft or bump to v1.0")`. [D-B4]
- [ ] `:122–127` — casesReviewed skipped under `--draft` (ok-line says so); under `--draft` with a resolvable run root require `gates.G2.defaults` non-empty, else `fail("C-E7-05: freeze --draft refused — gates.G2.defaults is empty (nothing defaulted, nothing to review)")`. RUN-POLICY checks (`:137–160`) run in both modes. [D-B4]
- [ ] `gate-check.mjs:5–12` header comment + `:1637–1649` usage — document `state … [--run <slug>]`, `freeze … [--draft] [--run <slug>]`. [D-B3, D-B4]
- [ ] Fixtures (each with README): `tests/fixtures/v4/triage-no-memo/.plan-it/t1.state.json` (verdict `skip`, no memo file; run `--run t1`) · `triage-bad-verdict/` (verdict `defer`) · `triage-closed/` (three variants `skip`/`build-instead`/`owner-decision` with memo files on disk) · `planreview-no-file/.plan-it/v4.state.json` (`gates.G4.approved:true`, `run.deliveryRoot: "delivery/v4/"`, no `delivery/v4/PLAN-REVIEW.md`) · `planreview-no-ack/` (file without `Reviewed-by:`) · `planreview-good/` (+ one contradicted default matched in G4) · `planreview-unmatched-contradiction/` · `draft-at-handoff/.plan-it/v4.state.json` (`state: "handoff"`, `contract.version: "1.0-draft"`) · `draft-header/delivery/CONTRACT.md` (`v1.0-draft` header, full structure + RUN-POLICY) · `draft-no-defaults/` (draft header, `gates.G2.defaults: []`) · `draft-no-reap/` (draft header, RUN-POLICY lacking `reap-on-merge`) · `defaults-empty/.plan-it/v4.state.json` (`defaultsApplied` in history, `defaults: []`) · `defaults-bad-source/` · `mode-mixed/` (autonomous + `decisionGate`; guided + `planReview`) · `contradiction-recovery/` (history with `REVIEW_CONTRADICTED`, one default contradicted once) · `contradiction-twice/` (same default twice: one variant with `escalated:true` + card, one without) · `state-run/` (`.plan-it/v4.state.json` with every optional §4.6 key populated). [D-B3, D-B4]
- [ ] `tests/v4/core/triage-closed.mjs`, `mode-consistency.mjs`, `state-run-flag.mjs`, `freeze-draft.mjs` — new positive harness scripts. [D-B3, D-B4]
- [ ] Mirror sync: plugin → root `scripts/gate-check.mjs`; `mirror-check` exit 0 before merge.

### Tier Table

| tier | effort | escalation | scaffold-pointer |
|---|---|---|---|
| top | statechart/gate semantics review of checks 3, 5, 6 (judgment slice) | already top; coordinator re-runs a failed slice, recorded | build-it:iteration-impl#slice=V4B2 |
| mid | spec'd-implementation vs this Test Contract (checks 1, 2, 4, 7; `freeze --draft`) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B2 |
| low | mechanical (state-file fixtures, README, mirror copy) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B2-fixtures |

Cascade map: partial-failure — T-V4B2-08 (a plan review with one contradicted default and one accepted default: the run lands partially contradicted, not all-or-nothing); rollback/compensation — T-V4B2-11 (REVIEW_CONTRADICTED re-enters `parallelPlanning` as an AMENDMENT, compensating the frozen draft); failed-recovery→escalation — T-V4B2-11 (a second contradiction of the same default is ESCALATED as an open card, never re-defaulted); recovery/resume — T-V4B2-12 (`state --run` resumes after a crash) and T-V4B2-03 (write the memo, retry, the transition is accepted); adversarial-verify — T-V4B2-06/07 (the verb re-reads PLAN-REVIEW.md from disk instead of trusting `gates.G4.approved`).

### Test Contract — V4B2 state verb + draft freeze   (BINDING: 100% pass or /iterate)
Types: [unit][integration] · Count: 15 · [REAL]: 0 · Surfaces: gate-check exit codes + node harness scripts, offline
DoD = every case below PASS (T-V4B2-01 … T-V4B2-15). No [REAL] case is VERIFIED on a mock.

| ID | @tag | Case (given / when / then → expected) | run: | fixture | Covers |
|---|---|---|---|---|---|
| T-V4B2-01 | @case-machine | Given a state with `triage.verdict: "defer"`; when `state --dir … --run t2`; then exit 1 (REJECTED) and stderr names `C-E6-01` and lists the four allowed verdicts | `node tests/v4/core/triage-closed.mjs` | `tests/fixtures/v4/triage-bad-verdict/` | C-E6-01 |
| T-V4B2-02 | @case-machine | Given verdict `skip`, `history` containing `triage`, no `triage.memo` on disk; when the CONTRACT `run:`; then exit 1 and stderr contains `CLOSED_WITHOUT_PLAN REJECTED` and `C-E6-01` (fix named: write the memo) | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs state --dir tests/fixtures/v4/triage-no-memo --run t1` | `tests/fixtures/v4/triage-no-memo/` | C-E6-01 |
| T-V4B2-03 | @case-machine | Given the same run with the memo written (recovery: fix + retry) for each of `skip`, `build-instead`, `owner-decision`; when `state`; then exit 0 and stdout contains `CLOSED_WITHOUT_PLAN` and `(final state)` | `node tests/v4/core/triage-closed.mjs` | `tests/fixtures/v4/triage-closed/` | C-E6-02 |
| T-V4B2-04 | @case-machine | Given `defaultsApplied` in history and `gates.G2.defaults: []`; when the CONTRACT `run:`; then exit 1 (REJECTED) naming `C-E7-07` | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs state tests/fixtures/v4/defaults-empty/.plan-it/v4.state.json` | `tests/fixtures/v4/defaults-empty/` | C-E7-07 |
| T-V4B2-05 | @case-machine | Given a defaults row with `source: "guessed"`; when `state`; then exit 1 naming the row id and `C-E7-07`; given all rows `source: "recommended"` with boolean `contradicted`; then exit 0 | `node tests/v4/core/mode-consistency.mjs` | `tests/fixtures/v4/defaults-bad-source/` | C-E7-07 |
| T-V4B2-06 | @case-machine | Given `gates.G4.approved: true` and no `delivery/v4/PLAN-REVIEW.md`; when the CONTRACT `run:`; then exit 1 (REJECTED) naming `C-E7-03` and the missing path | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs state --dir tests/fixtures/v4/planreview-no-file --run v4` | `tests/fixtures/v4/planreview-no-file/` | C-E7-03 |
| T-V4B2-07 | @case-machine | Given PLAN-REVIEW.md present without a `Reviewed-by: <name> <date>` line; when `state --dir … --run v4`; then exit 1 naming `C-E7-03` (B2 mirror) | `node tests/v4/core/mode-consistency.mjs` | `tests/fixtures/v4/planreview-no-ack/` | C-E7-03 |
| T-V4B2-08 | @case-machine | Given PLAN-REVIEW.md with the ack line, G2/G3 recorded, one default `contradicted:true` matched in `gates.G4.contradictions[]` and one `contradicted:false` (partial contradiction); when `state`; then exit 0; given a `contradicted:true` default with no matching G4 entry; then exit 1 naming the id | `node tests/v4/core/mode-consistency.mjs` | `tests/fixtures/v4/planreview-good/`, `tests/fixtures/v4/planreview-unmatched-contradiction/` | C-E7-03 |
| T-V4B2-09 | @case-machine | Given `state: "handoff"` and `contract.version: "1.0-draft"`; when the CONTRACT `run:`; then exit 1 (REJECTED) and stderr contains `draft contract cannot hand off` and `C-E7-04` (G-13); the same file with `version: "1.0"` exits 0 | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs state tests/fixtures/v4/draft-at-handoff/.plan-it/v4.state.json` | `tests/fixtures/v4/draft-at-handoff/` | C-E7-04 |
| T-V4B2-10 | @case-machine | Given `run.mode: "autonomous-draft"` with `decisionGate` in history, and separately `"guided"` with `planReview` in history; when `state`; then each exits 1 (REJECTED) naming `C-E7-06` and the offending state | `node tests/v4/core/mode-consistency.mjs` | `tests/fixtures/v4/mode-mixed/` | C-E7-06 |
| T-V4B2-11 | @case-machine | Given history `… freeze —REVIEW_CONTRADICTED→ parallelPlanning …` with one default contradicted once (rollback of the draft into an AMENDMENT); when `state`; then exit 0 and the recovery edge is listed; given the same default contradicted twice with `escalated:true` and a `card` on disk; then exit 0 and stdout prints `ESCALATED: <id> → <card>`; given twice without the card; then exit 1 `contradicted twice without ESCALATED card — never re-default` (`C-E7-06`) | `node tests/v4/core/mode-consistency.mjs` | `tests/fixtures/v4/contradiction-recovery/`, `tests/fixtures/v4/contradiction-twice/` | C-E7-06 |
| T-V4B2-12 | @case-machine | Given `.plan-it/v4.state.json` at `parallelPlanning` with every §4.6 optional key populated (`run.name/mode/topology/deliveryRoot/anamnesis`, `triage`, `gates.G0/G4`, `contract.draft`, `render`, `archive`); when `state --dir <fx> --run v4` (resume after crash); then exit 0, stdout `state: parallelPlanning`, next events `AMENDMENT, SQUADS_COMPLETE` (untagged, both modes) — extra keys tolerated; when the file is at `render` in autonomous-draft; then only `REVIEW_READY` is printed, never `RENDERED` | `node tests/v4/core/state-run-flag.mjs` | `tests/fixtures/v4/state-run/` | C-E9-01 |
| T-V4B2-13 | @case-machine | Given the `v1.0-draft` CONTRACT fixture; when `freeze` without `--draft` (the CONTRACT `run:`); then exit 1 and stderr contains `draft cannot be final` and `C-E7-05` (G-13) | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs freeze tests/fixtures/v4/draft-header/delivery/CONTRACT.md` | `tests/fixtures/v4/draft-header/` | C-E7-05 |
| T-V4B2-14 | @case-machine | Given the same fixture with a run root whose `casesReviewed` is false and `gates.G2.defaults` non-empty; when `freeze --draft --dir <fx>`; then exit 0 and stdout says casesReviewed was skipped; given `draft-no-defaults/`; then exit 1 `freeze --draft refused — gates.G2.defaults is empty`; given `draft-no-reap/`; then exit 1 `RUN-POLICY body missing the "reap-on-merge"` (structure still checked under `--draft`) | `node tests/v4/core/freeze-draft.mjs` | `tests/fixtures/v4/draft-header/`, `tests/fixtures/v4/draft-no-defaults/`, `tests/fixtures/v4/draft-no-reap/` | C-E7-05 |
| T-V4B2-15 | @case-machine | Regression: given the v2 fixture `tests/fixtures/state-valid.json` and the v3 state fixtures (`gaps-*`, `credentials-*`, `no-review-file`, `review-no-ack`); when `state` with the 4.0.0 machine; then every exit code and reason string equals the 3.0.1 output byte-for-byte (the new checks never fire without their keys) | `node tests/v4/core/state-run-flag.mjs` | `tests/fixtures/state-valid.json`, `tests/fixtures/v3/*` | C-E11-04 |

---

## Epic V4B3 — Named runs: `resolveStateFile`, deliveryRoot-aware verbs, `archive`, `runs`, guard resolution in both copies, grammar widening + v4 CONTRACT discovery

Branch: `epic/v4b-named-runs` (worktree off `main`)
Depends on: V4B2. Must be merged before QA runs the v4 Cases (C-E9-10, program plan §2).
Covers: C-E9-02, C-E9-03, C-E9-04, C-E9-05, C-E9-06, C-E9-07, C-E9-08, C-E9-10
Scope: one state file per run and every verb routing through it (PRD D-B5, D-B6); the guard resolving the governing run by longest `deliveryRoot` prefix in BOTH shipped copies (D-B7); `archive`/`runs` (D-B8); the harness grammar widened to `C-E\d+-\d{2}` and `V4B1`-shaped epic headings, with `delivery/v4/CONTRACT.md` discovered (D-B13 discovery half).

### Task checklist
- [ ] `gate-check.mjs` after `:1084` — `export function resolveStateFile(root, slug)` (explicit slug → named; else exactly one `*.state.json` and no generic → it; else generic) and `parseRunFlag`. `:1086–1092` `readState(root, slug)`. `:1102–1111` `resolveRunRoot(path)` also matches `<root>/<deliveryRoot>/CONTRACT.md` by scanning `<root>/.plan-it/*.state.json` (excluding `done/`) for a `run.deliveryRoot` that joins to the contract's directory. [D-B5]
- [ ] `gate-check.mjs` new `resolveDeliveryDir(root, state)` (deliveryRoot → `delivery/` if it carries `CONTRACT.md|prds/|epics/` → `delivery/v3/`); apply at `:90` (`cmdFreeze --dir` candidates), `:525` (`cmdAdversary`), `:1168` (`cmdContract --dir`), `:1322`/`:1328`/`:1351` (`reconcileScan` epics/prds/CONTRACT), `:1385` (review file) and `:1403` (decisions.md) with `<root>/delivery/` fallbacks; `cmdHandoff :868–872` passes root + slug to `reconcileScan`. `--run <slug>` accepted by `contract|reconcile|adversary|freeze|state|testconv`. [D-B6]
- [ ] `gate-check.mjs:1272–1276` (`cmdTestconv.writeReceipt`) — write to `resolveStateFile(dir, slug)`; never create a generic file when a named one exists; `:1280/:1301/:1308` messages name the resolved file. [D-B5]
- [ ] `gate-check.mjs` before `:1616` — `cmdArchive([slug, ...])`: `--dir root`; state ∈ {`done`, `closedWithoutPlan`} else exit 1 `ARCHIVE_REFUSED: refuses to archive a run in state <s> (C-E9-05)`; target exists ⇒ exit 1 `ARCHIVE_REFUSED: .plan-it/done/<slug>.state.json exists`; `--force` ⇒ usage error exit 1 (`no --force: finish the run or record a triage verdict`); else `mkdirSync(done)`, `renameSync`, append `archive: {archivedAt: ISO, from: ".plan-it/<slug>.state.json"}`. [D-B8]
- [ ] before `:1616` — `cmdRuns(args)`: `--dir root`, `--json`; rows from `.plan-it/*.state.json` + `.plan-it/done/*.state.json`; columns `slug | state | mode | contract | last transition | deliveryRoot | archived`; computed `N run(s) (M archived)` line; zero rows ⇒ exit 0 `no plan-it runs under <root>`; unparseable file ⇒ listed as `ERROR <file>: <reason>`, others still listed, exit 1. `--json` emits `{runs:[…], counts:{live, archived}}`. [D-B8]
- [ ] `guard:105–118` — replace the `docs/implementation/<name>/` block with the D-B7 resolution (scan `.plan-it/*.state.json` excluding `done/`, parse each in its own try, longest `run.deliveryRoot` prefix wins, then legacy `docs/implementation/<name>/`, then generic, then allow); `guard:124–130` deny reason names the RESOLVED file and its `state`; everything else byte-untouched (fail-open frame `:40/:132`, `DELIVERABLE_RE :21`, W4/W3 blocks `:48–103`). Then `cp` plugin → root. [D-B7]
- [ ] `gate-check.mjs:1378` (`reconcileScan` binding-row grammar) — `C-(?:W\d+|META|E\d+)-\d{2}`; `:752`, `:1369` and `checkEpicTierTable :628` epic-heading grammar `[A-Z]\d+` → `[A-Z]{1,3}\d+[A-Z]?\d*` (`V4B1` seen; `E1`, `A2`, `B3` unchanged). [D-B13]
- [ ] `tests/v3/lib/contract-cases.mjs:21` — `CONTRACT_PATHS = [v3, v4]`; `parseContractCases(text?)` keeps its v3 default; new `parseAllContractCases()` returns rows with `source`; `rowRe` ID group unchanged (already `[A-Z][A-Z0-9-]*`); `refPaths` also matches `tests/v4/` path literals (same shape as its `tests/v3/` alternative); `mechanismGap` unchanged otherwise. [D-B13]
- [ ] `gate-check.mjs:5–12` header + `:1616–1629` dispatch (`archive`, `runs`) + `:1637–1649` usage lines. [D-B8]
- [ ] Fixtures (each with README): `tests/fixtures/v4/portfolio/.plan-it/{alpha.state.json (state done), beta.state.json (state discovery)}` · `portfolio-archived/` (one already in `done/`) · `portfolio-broken/` (one unparseable file) · `deliveryroot-v4-only/` (`.plan-it/v4.state.json` with `run.deliveryRoot: "delivery/v4/"`, `delivery/v4/{CONTRACT.md with a typed tally that disagrees, epics/epics-x.md with an "## Epic X1 —" section and zero case rows, prds/}`, `delivery/v3/` present and empty) · `deliveryroot-thin/` (a thin-machine CONTRACT under `delivery/v4/` for `adversary`) · `testconv-named/` (CLAUDE.md with the conventions block, `.plan-it/v4.state.json`, no generic) · `guard-two-runs/.plan-it/{state.json (contract null, state discovery), v4.state.json (contract "1.0-draft", deliveryRoot "delivery/v4/")}` · `guard-unfrozen-named/` (only `v4.state.json`, `contract.version: null`; a `-frozen` twin with `"1.0"`) · `guard-legacy-docs/` (`docs/implementation/eng/` layout + `.plan-it/eng.state.json`) · `guard-broken-named/` (`.plan-it/x.state.json` = `{not json`) · `epic-heading-v4/` (`delivery/epics/e.md` with `## V4B1 — sample` and no Tier Table). [D-B5–D-B8, D-B13]
- [ ] `tests/v4/core/resolve-state-file.mjs`, `testconv-named.mjs`, `deliveryroot-resolution.mjs`, `archive-moves.mjs`, `runs-list.mjs`, `guard-named-run.mjs`, `guard-unfrozen-named.mjs`, `contract-cases-v4.mjs`, `epic-heading-grammar.mjs` — new scripts; guard scripts spawn BOTH `scripts/hooks/planit-guard.mjs` and `plugins/plan-it/scripts/hooks/planit-guard.mjs` with `cwd` = fixture dir (no writes) and assert identical decisions. [D-B7]
- [ ] Mirror sync: plugin → root for `gate-check.mjs` and `planit-guard.mjs`; `mirror-check` exit 0 before merge.

### Tier Table

| tier | effort | escalation | scaffold-pointer |
|---|---|---|---|
| top | guard resolution semantics + fail-open proof (judgment slice) | already top; coordinator re-runs a failed slice, recorded | build-it:iteration-impl#slice=V4B3-guard |
| mid | spec'd-implementation vs this Test Contract (`resolveStateFile`, deliveryRoot sites, `archive`, `runs`, grammar widening) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B3 |
| low | mechanical (portfolio/guard fixtures, README, mirror copy) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B3-fixtures |

Cascade map: partial-failure — T-V4B3-12 (`runs` with one unparseable file: the others are listed, the bad one is named, exit 1 — a partial result, never all-or-nothing); rollback/compensation — T-V4B3-09/10 (ARCHIVE_REFUSED leaves the source byte-identical: nothing half-applied; no `--force` to force it); failed-recovery→escalation — N/A for this epic (waiver: `failed-recovery→escalation: N/A — archive/runs/guard have no retry loop; escalation is asserted in V4B2 and V4B4`); recovery/resume — T-V4B3-14 (WRITE_DENIED → record `contract.version` → the same write is allowed) and T-V4B3-02 (the FD-1 receipt lands in the named file so `state --run` sees it on resume); adversarial-verify — T-V4B3-13 (the fix is asserted in BOTH guard copies, the mirror is not trusted) and T-V4B3-11 (`runs --json` is re-parsed and round-tripped against the table).

### Test Contract — V4B3 named runs   (BINDING: 100% pass or /iterate)
Types: [unit][integration] · Count: 17 · [REAL]: 0 · Surfaces: gate-check exit codes + hook stdin/stdout + node harness scripts, offline
DoD = every case below PASS (T-V4B3-01 … T-V4B3-17). No [REAL] case is VERIFIED on a mock.

| ID | @tag | Case (given / when / then → expected) | run: | fixture | Covers |
|---|---|---|---|---|---|
| T-V4B3-01 | @case-machine | Given roots with (a) only `v4.state.json`, (b) `v4.state.json` + `state.json`, (c) two named files and no generic, (d) nothing; when `resolveStateFile(root, slug?)` is imported and called; then (a) no slug → the named file, (b) no slug → generic, (c) no slug → generic path (ambiguous, never guessed) and `slug:"v4"` → named, (d) → generic path; an explicit missing slug is reported as missing, never silently swapped | `node tests/v4/core/resolve-state-file.mjs` | `tests/fixtures/v4/state-run/`, `tests/fixtures/v4/portfolio/`, temp dirs | C-E9-02 |
| T-V4B3-02 | @case-machine | Given `testconv-named/` (conventions block present, only `.plan-it/v4.state.json`); when `testconv --dir <tmp copy> --run v4`; then exit 0, the receipt `testConventions.registered:true` is in `v4.state.json` and `.plan-it/state.json` does NOT exist afterwards (the generic file is never created beside a named one) | `node tests/v4/core/testconv-named.mjs` | `tests/fixtures/v4/testconv-named/` | C-E9-02 |
| T-V4B3-03 | @case-machine | Given the same copy without `--run`; when `testconv --dir <tmp>`; then the single named file is resolved and written, generic still absent; given a root with both files and no `--run`; then the generic file is written (legacy behaviour preserved) | `node tests/v4/core/testconv-named.mjs` | `tests/fixtures/v4/testconv-named/` | C-E9-02 |
| T-V4B3-04 | @case-machine | Given `deliveryroot-v4-only/` (`delivery/v4/epics/` holds an epic with zero case rows; `delivery/v3/` empty); when `reconcile --dir <fx> --run v4`; then exit 1 naming `C-W5-03` and the v4 epic file — the scan followed `run.deliveryRoot` (3.0.1 exits 0 here: the F-B14 false green) | `node tests/v4/core/deliveryroot-resolution.mjs` | `tests/fixtures/v4/deliveryroot-v4-only/` | C-E9-03 |
| T-V4B3-05 | @case-machine | Given the same fixture (its `delivery/v4/CONTRACT.md` carries a typed tally that disagrees with the row count); when `contract --dir <fx> --run v4`; then exit 1 naming `C-W5-01` and `delivery/v4/CONTRACT.md` (not `delivery/v3/`) | `node tests/v4/core/deliveryroot-resolution.mjs` | `tests/fixtures/v4/deliveryroot-v4-only/` | C-E9-03 |
| T-V4B3-06 | @case-machine | Given `deliveryroot-thin/` (thin machine in `delivery/v4/CONTRACT.md`); when `adversary --dir <fx> --run v4`; then exit 1 with `D-B3` cascade gaps named — proving the v4 contract was read; `adversary <fx>/delivery/v4` positional agrees | `node tests/v4/core/deliveryroot-resolution.mjs` | `tests/fixtures/v4/deliveryroot-thin/` | C-E9-03 |
| T-V4B3-07 | @case-machine | Given `deliveryroot-v4-only/`; when `freeze --dir <fx> --run v4 --draft`; then the contract at `delivery/v4/CONTRACT.md` is the one checked (its header is named in output); when positional `freeze <fx>/delivery/v4/CONTRACT.md --draft`; then `resolveRunRoot` finds the run through `deliveryRoot` and the casesReviewed/defaults rules apply (run-backed mode) | `node tests/v4/core/deliveryroot-resolution.mjs` | `tests/fixtures/v4/deliveryroot-v4-only/` | C-E9-03 |
| T-V4B3-08 | @case-machine | Given a temp copy of `portfolio/`; when `archive alpha --dir <tmp>`; then exit 0, `.plan-it/done/alpha.state.json` exists with `archive.archivedAt` (ISO date) and `archive.from`, the source is gone, and every other key is byte-equal to the original | `node tests/v4/core/archive-moves.mjs` | `tests/fixtures/v4/portfolio/` | C-E9-04 |
| T-V4B3-09 | @case-machine | Given `portfolio/` (beta at `discovery`); when the CONTRACT `run:`; then exit 1, stderr contains `ARCHIVE_REFUSED` and `refuses to archive a run in state discovery` and `C-E9-05`, and `beta.state.json` is byte-identical afterwards (nothing half-applied) | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs archive beta --dir tests/fixtures/v4/portfolio` | `tests/fixtures/v4/portfolio/` | C-E9-05 |
| T-V4B3-10 | @case-machine | Given `portfolio-archived/` (a `done/alpha.state.json` already present) and a live `alpha.state.json`; when `archive alpha`; then exit 1 `ARCHIVE_REFUSED … exists` and neither file changes; when `archive beta --force`; then exit 1 usage error naming `no --force` (the flag does not exist) | `node tests/v4/core/archive-moves.mjs` | `tests/fixtures/v4/portfolio-archived/`, `tests/fixtures/v4/portfolio/` | C-E9-05 |
| T-V4B3-11 | @case-machine | Given `portfolio/`; when `runs --dir <fx>`; then exit 0, exactly two rows (`alpha … done`, `beta … discovery`) and a computed `2 run(s) (0 archived)` line; when `runs --dir <fx> --json`; then stdout parses and its rows round-trip to the same slugs/states as the table (adversarial-verify: re-read, not trusted); given an empty temp dir; then exit 0 and `no plan-it runs under` | `node tests/v4/core/runs-list.mjs` | `tests/fixtures/v4/portfolio/` | C-E9-06 |
| T-V4B3-12 | @case-machine | Given `portfolio-broken/` (one file `{not json`); when `runs --dir <fx>`; then exit 1, the good run is still listed and the broken file is named with `ERROR` (partial failure, named); `portfolio-archived/` lists the archived run with `archived: yes` and counts `1 archived` | `node tests/v4/core/runs-list.mjs` | `tests/fixtures/v4/portfolio-broken/`, `tests/fixtures/v4/portfolio-archived/` | C-E9-06 |
| T-V4B3-13 | @case-guard | Given `guard-two-runs/` (frozen v4 named run at `delivery/v4/`, unfrozen generic); when hook stdin `Write <fx>/delivery/v4/prds/prd-1.md` is piped to `scripts/hooks/planit-guard.mjs` AND to `plugins/plan-it/scripts/hooks/planit-guard.mjs` with `cwd:<fx>`; then both allow (empty stdout); when `Write <fx>/delivery/other/prds/prd-1.md`; then both deny (WRITE_DENIED) with a reason naming `.plan-it/state.json` — the longest `deliveryRoot` prefix wins, asserted in BOTH copies | `node tests/v4/core/guard-named-run.mjs` | `tests/fixtures/v4/guard-two-runs/` | C-E9-07 |
| T-V4B3-14 | @case-guard | Given `guard-unfrozen-named/` (only `v4.state.json`, `contract.version: null`); when `Write <fx>/delivery/v4/prds/prd-1.md`; then WRITE_DENIED and the reason names `v4.state.json` (not the generic name); given the `-frozen` twin (`contract.version: "1.0"`, recovery: record the version and retry); then the same write is allowed — both copies | `node tests/v4/core/guard-unfrozen-named.mjs` | `tests/fixtures/v4/guard-unfrozen-named/` | C-E9-08 |
| T-V4B3-15 | @case-guard | Regression + fail-open: given `guard-legacy-docs/`; when `Write <fx>/docs/implementation/eng/prds/p.md`; then the legacy match still resolves `eng.state.json`; given `guard-broken-named/`; then allow (fail-open on an unparseable named file); given the v2 harness fixtures T-E6-01…05 (`guardCwd`); then identical decisions to 3.0.1 — both copies | `node tests/v4/core/guard-named-run.mjs` | `tests/fixtures/v4/guard-legacy-docs/`, `tests/fixtures/v4/guard-broken-named/` | C-E9-07 |
| T-V4B3-16 | @case-machine | Given `contract-cases.mjs`; when `parseAllContractCases()` runs; then it returns the v3 rows plus every `C-E\d+-\d{2}` row of `delivery/v4/CONTRACT.md` tagged `source: delivery/v4/CONTRACT.md`, the count equals the number of `| C-E` rows in that file (computed), `parseContractCases()` still returns only v3 rows, and `refPaths("node tests/v4/core/x.mjs")` yields the path; `node tests/run-contract.mjs` output contains a `-- v4` section header with computed totals | `node tests/v4/core/contract-cases-v4.mjs` | `delivery/v4/CONTRACT.md`, `delivery/v3/CONTRACT.md` | C-E9-10 |
| T-V4B3-17 | @case-machine | Given `epic-heading-v4/` (an epic headed `## V4B1 — sample` with no Tier Table); when `reconcile --dir <fx>`; then exit 1 naming `C-W3-01` and `V4B1` (3.0.1 exits 0: the heading was invisible); given the v3 fixtures `tier-table-good`/`no-tier`/`epic-no-cases`; then their exit codes are unchanged | `node tests/v4/core/epic-heading-grammar.mjs` | `tests/fixtures/v4/epic-heading-v4/`, `tests/fixtures/v3/{tier-table-good,no-tier,epic-no-cases}/` | C-E9-10 |

---

## Epic V4B4 — `mirror`, `glossary`, disposition counting in `reconcile`, ENV-FACTS tool-only fix — all embedded in `handoff` as scoped steps

Branch: `epic/v4b-lints` (worktree off `main`)
Depends on: V4B3 (deliveryRoot resolution feeds `handoff`'s embedded scans). Independent of SQ-A: the mirror fixtures are hand-stamped per CONTRACT §4.3.
Covers: C-E2-05, C-E2-06, C-E2-07, C-E6-03, C-E8-01, C-E8-02, C-E8-03, C-E10-01, C-E10-02
Scope: PRD D-B9 (`mirror`), D-B10 (`glossary` + `state` requires GLOSSARY.md at handoff for v4 runs), D-B11 (dispositions in `reconcileScan`), D-B12 (ENV-FACTS blacklists one tool), and the `handoff` steps 8 and 9 that embed them with one shared `failures[]` and one `finish()` (C-W5-04 pattern, F-B15).

### Task checklist
- [ ] `gate-check.mjs` after `:1613` — `cmdMirror(args)`: `mirror <md> <html>` and `mirror --dir <delivery> [--require-html]`; stamp parser for `planit-source`, `planit-embeds`, `planit-brand` (`default` ⇒ `join(here, "..", "assets", "brand", "default.brand.json")`; `repo:<relpath>` relative to the twin's dir); SHA-256 via `node:crypto` over raw bytes; exit 0 MIRROR_FRESH / 2 MIRROR_STALE (message names `stamped <hash> vs current <hash>` and the changed input) / 1 HTML_UNSTAMPED ⇒ MIRROR_REJECTED or md missing; `--dir` walks `*.html`, family basenames SCOPE-BRIEF · TRIAGE · DECISIONS · CONTRACT · KICKOFF · PLAN-REVIEW · GLOSSARY without a twin are reported and fail with exit 2 `missing twin` only under `--require-html` (`C-E2-05`); every message carries `C-E2-05`/`C-E2-06`/`C-E2-07` as applicable. Import `createHash`. [D-B9]
- [ ] `gate-check.mjs` after `:906` — `cmdHandoff` **step 9 (mirror)**: fires when a stamped `.html` exists under the dir or the resolved run's `machineVersion` major ≥ 4 (then `--require-html`, G-6); stale twin whose on-disk SHA-256 equals a `render.outputs[].sha256` recorded in state ⇒ `fail("ESCALATED (C-E2-07): <twin> still stale after re-render — the md changed shape; update the manifest")`, else `fail("MIRROR_STALE (C-E2-06): <twin> — re-render")`. Same `failures[]`, one `finish()`. [D-B9]
- [ ] `gate-check.mjs` before `:1616` — `cmdGlossary([dir])`: scan `KICKOFF.md`, `DECISIONS.md`, `STATUS.md`, `SESSIONS.md`, `GATE.md`, `PLAN-REVIEW.md`, `SCOPE-BRIEF.md`, `00-program-plan.md` when present; `stripCode` first; token regex and stoplist per D-B10; `GLOSSARY.md` rows `| ID | Expansion | Where defined |` with cell items split on `·`/`,`, numeric ranges (`R1 … R12`) expanded, family patterns (`*` ⇒ `[A-Za-z0-9.]+`, `NN` ⇒ `[A-Z0-9]{2,3}`, `<n>`/trailing `n` ⇒ `\d+`); exit 1 lists every unknown as `<file>:<line>: unknown ID "<tok>" — add a GLOSSARY.md row (C-E10-01)`; missing `GLOSSARY.md` ⇒ exit 1 naming it. [D-B10]
- [ ] `gate-check.mjs` after `:906` — `cmdHandoff` **step 8 (glossary)**: scoped — fires when `GLOSSARY.md` exists under the dir (or `machineVersion` major ≥ 4, then its absence fails `C-E10-02`); shares `failures[]`. [D-B10]
- [ ] `gate-check.mjs` after `:978` (`cmdState`) — when `state ∈ {handoff, done}` and `machineVersion` major ≥ 4: `<root>/<deliveryRoot ?? "delivery/">GLOSSARY.md` must exist, else `fail("C-E10-02 (G-8): GLOSSARY.md missing at handoff …")`; v3 `machineVersion` never triggers. [D-B10]
- [ ] `gate-check.mjs` after `:1419` (`reconcileScan`) — disposition block, scoped on a `Disposition` column or `## Residuals` heading in `<deliveryDir>/STATUS.md`: locate `Status` and `Disposition` columns by header name; grammar `^(backlog-with-reason: \S.*|owner-gated: \S.*|IMPLEMENTED-NOT-VERIFIED: \S+ \S.*)$`; non-VERIFIED row with empty/malformed cell ⇒ `fail("C-E8-01 (G-3): STATUS row <EID> is <status> with no disposition …")`; VERIFIED row with a non-empty non-`—` cell ⇒ fail; `backlog-with-reason` path must exist under root ⇒ else fail; `## Residuals` rows `| Item | Disposition | Reason / exit criterion | Evidence |` — Item `^(T|C)-` with `backlog-with-reason` ⇒ `fail("C-E8-02 (G-9): contract cases never move to backlog — <item>")`; typed `Dispositions: N backlog · M owner-gated · K INV` ≠ computed ⇒ `fail("C-E8-03 (G-5): typed dispositions N/M/K disagree with computed n/m/k")`; `## Log` section (incl. `[incidental]` bullets) excluded from every count. [D-B11]
- [ ] `gate-check.mjs:1138–1155` (`unavailableToolsFromEnvFacts`) — read an optional `tool` column (5th cell); blacklist `tool` when present else `check` argv[0]; never the remaining tokens. `:190–204` (`envFactsMarkdown`) — header `| id | check | status | evidence | tool |`, `tool` cell from the probe's `tool` field (empty when undeclared); probe table rows in `cmdPreflight` (`:206–275`) gain `tool` where the probe wraps another tool. Message at `:1218` carries `C-E6-03`. [D-B12]
- [ ] `gate-check.mjs:5–12` header + dispatch `:1616–1629` (`mirror`, `glossary`) + usage `:1637–1649`. [D-B9, D-B10]
- [ ] Fixtures (each with README; twins hand-stamped per CONTRACT §4.3 with hashes computed by a small `tests/fixtures/v4/mirror/_stamp.mjs` helper, checked in): `tests/fixtures/v4/mirror-fresh/{X.md, X.html}` · `mirror-stale/{CONTRACT.md, CONTRACT.html}` (stamp computed before one byte changed) + `CONTRACT.rerendered.html` (fresh stamp, used by the re-render half) · `mirror-unstamped/{X.md, X.html}` (no meta) · `mirror-malformed/` (bad hex length) · `mirror-md-missing/` · `mirror-embed-changed/` (`planit-embeds` hash stale) · `mirror-brand-repo/` (`repo:brand/brand.json` fresh and stale variants) · `mirror-missing-twin/delivery/{KICKOFF.md (no html), notes.md (no html), CONTRACT.md+CONTRACT.html}` · `mirror-escalated/` (a run root: `.plan-it/r.state.json` with `render.outputs[]` sha256 equal to the on-disk stale twin; `delivery/` with GLOSSARY.md) · `glossary-unknown-id/delivery/{KICKOFF.md (mentions X9-77 and T-A4-B1), GLOSSARY.md (T-*-NN row, no X9-77)}` · `glossary-good/delivery/` (ranges, `·` lists, `<n>` families, code-span mentions) · `glossary-missing/` · `disposition-missing/delivery/STATUS.md` · `disposition-contract-case/` (`## Residuals` row `T-B2-04 | backlog-with-reason: …`) · `disposition-tally-drift/` (typed tally off by one + `[incidental]` log bullets naming case IDs) · `disposition-good/` (well-formed, path exists, tally equal) · `disposition-bad-path/` · `disposition-malformed/` · `envfacts-tool-only/` (ENV-FACTS.md: `node scripts/check-gh.mjs` ABSENT with `tool: gh`; CONTRACT cases: one `node …`, one `gh …`). [D-B9–D-B12]
- [ ] `tests/v4/core/mirror-stale.mjs`, `mirror-escalated.mjs`, `glossary-lint.mjs`, `glossary-handoff.mjs`, `disposition-good.mjs`, `envfacts-tool-only.mjs` — new scripts. [D-B9–D-B12]
- [ ] Mirror sync: plugin → root `gate-check.mjs`; `mirror-check` exit 0 before merge.

### Tier Table

| tier | effort | escalation | scaffold-pointer |
|---|---|---|---|
| top | ESCALATED detection + disposition/glossary grammar review (judgment slice) | already top; coordinator re-runs a failed slice, recorded | build-it:iteration-impl#slice=V4B4-review |
| mid | spec'd-implementation vs this Test Contract (`mirror`, `glossary`, disposition block, ENV-FACTS fix, handoff steps 8–9) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B4 |
| low | mechanical (hand-stamped twins via the checked-in stamp helper, STATUS/GLOSSARY fixtures, README, mirror copy) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B4-fixtures |

Cascade map: partial-failure — T-V4B4-06 (`mirror --dir` over a package where some twins are fresh and one md has no twin: the fresh ones pass, the missing one is named — a partial result); rollback/compensation — N/A for this epic (waiver: `rollback/compensation: N/A — every lint here is read-only; nothing to undo`); failed-recovery→escalation — T-V4B4-07 (MIRROR_STALE after a re-render that produced the same bytes ⇒ ESCALATED in the handoff output); recovery/resume — T-V4B4-02 (MIRROR_STALE → re-render → MIRROR_FRESH) and T-V4B4-14 (a malformed disposition is fixed and `reconcile` re-verifies to exit 0); adversarial-verify — T-V4B4-02/04/05 (the stamp is not trusted: md, embed and brand bytes are re-hashed and a tampered md is caught).

### Test Contract — V4B4 mirror · glossary · dispositions · ENV-FACTS   (BINDING: 100% pass or /iterate)
Types: [unit][integration] · Count: 16 · [REAL]: 0 · Surfaces: gate-check exit codes + node harness scripts, offline
DoD = every case below PASS (T-V4B4-01 … T-V4B4-16). No [REAL] case is VERIFIED on a mock.

| ID | @tag | Case (given / when / then → expected) | run: | fixture | Covers |
|---|---|---|---|---|---|
| T-V4B4-01 | @case-machine | Given `mirror-fresh/` (stamp hash equals the md's SHA-256); when `mirror X.md X.html`; then exit 0 and stdout `MIRROR_FRESH` with the 12-hex prefix of the hash | `node tests/v4/core/mirror-stale.mjs` | `tests/fixtures/v4/mirror-fresh/` | C-E2-06 |
| T-V4B4-02 | @case-machine | Given `mirror-stale/` (one byte of CONTRACT.md changed after the stamp — tampered source); when the CONTRACT `run:`; then exit 2 (MIRROR_STALE) naming both hashes and `C-E2-06`; when the re-rendered twin replaces the stale one (recovery is mechanical); then `mirror` exits 0 MIRROR_FRESH | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs mirror tests/fixtures/v4/mirror-stale/CONTRACT.md tests/fixtures/v4/mirror-stale/CONTRACT.html` | `tests/fixtures/v4/mirror-stale/` | C-E2-06 |
| T-V4B4-03 | @case-machine | Given `mirror-unstamped/` (hand-authored HTML, no `planit-source` meta); when the CONTRACT `run:`; then exit 1 and stderr contains `HTML_UNSTAMPED`, `MIRROR_REJECTED` and `C-E2-07` — the HTML is never the source | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs mirror tests/fixtures/v4/mirror-unstamped/X.md tests/fixtures/v4/mirror-unstamped/X.html` | `tests/fixtures/v4/mirror-unstamped/` | C-E2-07 |
| T-V4B4-04 | @case-machine | Given `mirror-malformed/` (stamp with a 40-hex hash) and `mirror-md-missing/` (stamp relpath points nowhere); when `mirror`; then each exits 1 with the structural reason named (`malformed stamp` / `source not found`), never exit 2 | `node tests/v4/core/mirror-stale.mjs` | `tests/fixtures/v4/mirror-malformed/`, `tests/fixtures/v4/mirror-md-missing/` | C-E2-07 |
| T-V4B4-05 | @case-machine | Given `mirror-embed-changed/` (source fresh, one `planit-embeds` entry stale) and `mirror-brand-repo/` (`planit-brand` = `repo:brand/brand.json`, fresh and stale variants); when `mirror`; then embed-stale exits 2 naming the embed relpath, brand-fresh exits 0, brand-stale exits 2 naming `brand` — relpaths resolved against the twin's directory (CONTRACT §4.3) | `node tests/v4/core/mirror-stale.mjs` | `tests/fixtures/v4/mirror-embed-changed/`, `tests/fixtures/v4/mirror-brand-repo/` | C-E2-06 |
| T-V4B4-06 | @case-machine | Given `mirror-missing-twin/delivery/` (CONTRACT twin fresh; `KICKOFF.md` family-kind with no html; `notes.md` non-family with no html); when the CONTRACT `run:` (`--require-html`); then exit 2 with `missing twin: KICKOFF.md` and `C-E2-05`, `notes.md` not named, the fresh pair reported fresh (partial result); without `--require-html` the same dir exits 0 and reports the missing twin as a note | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs mirror --dir tests/fixtures/v4/mirror-missing-twin/delivery --require-html` | `tests/fixtures/v4/mirror-missing-twin/` | C-E2-05 |
| T-V4B4-07 | @case-machine | Given `mirror-escalated/` (a v4 run whose `render.outputs[]` records the SHA-256 of the on-disk twin, and that twin is stale against its md); when `handoff <fx>/delivery --dir <fx>` (or `handoff --dir <fx> --run r`); then exit 1 and stderr contains `ESCALATED` and `still stale after re-render` and `C-E2-07`; the same package with a stale twin NOT matching `render.outputs[]` prints `MIRROR_STALE … re-render` instead | `node tests/v4/core/mirror-escalated.mjs` | `tests/fixtures/v4/mirror-escalated/` | C-E2-07 |
| T-V4B4-08 | @case-machine | Given `glossary-unknown-id/delivery/` (KICKOFF mentions `X9-77` with no row, and `T-A4-B1` covered by the `T-*-NN` family row); when the CONTRACT `run:`; then exit 1 naming `X9-77` with `KICKOFF.md:<line>` and `C-E10-01`, and `T-A4-B1` is NOT reported | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs glossary tests/fixtures/v4/glossary-unknown-id/delivery` | `tests/fixtures/v4/glossary-unknown-id/` | C-E10-01 |
| T-V4B4-09 | @case-machine | Given `glossary-good/delivery/` (rows with ranges `R1 … R12`, `·` lists `AMD-4 · AMD-5`, families `C-E<n>-NN`, `V4B<n>`, and a KICKOFF whose only unknown-shaped tokens sit inside code spans); when `glossary`; then exit 0 and the ok-line reports the computed count of IDs resolved; given `glossary-missing/`; then exit 1 naming `GLOSSARY.md` | `node tests/v4/core/glossary-lint.mjs` | `tests/fixtures/v4/glossary-good/`, `tests/fixtures/v4/glossary-missing/` | C-E10-01 |
| T-V4B4-10 | @case-machine | Given `glossary-unknown-id/delivery/`; when the CONTRACT `run:` (`handoff`); then exit 1 and the same `X9-77 … C-E10-01` line appears once (embedded, never double-reported) alongside `C-E10-02`; given a v4 state at `handoff` (`machineVersion: "4.0.0"`) with no `GLOSSARY.md` under its deliveryRoot; when `state --dir … --run r`; then exit 1 naming `C-E10-02`; the same with `machineVersion: "3.0.1"` exits 0 | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs handoff tests/fixtures/v4/glossary-unknown-id/delivery` | `tests/fixtures/v4/glossary-unknown-id/`, `tests/fixtures/v4/glossary-missing/` | C-E10-02 |
| T-V4B4-11 | @case-machine | Given `disposition-missing/` (board with a `Disposition` column, one `IN-PROGRESS` row with an empty cell); when the CONTRACT `run:`; then exit 1 naming the row's EID and `C-E8-01` (G-3) | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs reconcile --dir tests/fixtures/v4/disposition-missing` | `tests/fixtures/v4/disposition-missing/` | C-E8-01 |
| T-V4B4-12 | @case-machine | Given `disposition-contract-case/` (`## Residuals` row Item `T-B2-04`, disposition `backlog-with-reason: …`); when the CONTRACT `run:`; then exit 1 with `contract cases never move to backlog` and `C-E8-02` (G-9) | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs reconcile --dir tests/fixtures/v4/disposition-contract-case` | `tests/fixtures/v4/disposition-contract-case/` | C-E8-02 |
| T-V4B4-13 | @case-machine | Given `disposition-tally-drift/` (typed `Dispositions: 3 backlog · 1 owner-gated · 1 INV`, computed 2/1/1, plus `## Log` bullets tagged `[incidental]` that mention case IDs); when the CONTRACT `run:`; then exit 1 with both tallies printed and `C-E8-03` (G-5), and the `[incidental]` bullets change no count | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs reconcile --dir tests/fixtures/v4/disposition-tally-drift` | `tests/fixtures/v4/disposition-tally-drift/` | C-E8-03 |
| T-V4B4-14 | @case-machine | Given `disposition-good/` (VERIFIED rows empty/`—`, one `owner-gated: Fernando Ott`, one `IMPLEMENTED-NOT-VERIFIED: T-V4A1-03 cdnjs unreachable`, one `backlog-with-reason: delivery/backlog/x.md` whose path exists, typed tally equal); when `reconcile --dir`; then exit 0; given `disposition-bad-path/` (backlog path absent); then exit 1 naming the path; given `disposition-malformed/` (`deferred: later`); then exit 1 naming the closed grammar — and after the cell is corrected in a temp copy, `reconcile` re-verifies to exit 0 (recovery) | `node tests/v4/core/disposition-good.mjs` | `tests/fixtures/v4/disposition-good/`, `tests/fixtures/v4/disposition-bad-path/`, `tests/fixtures/v4/disposition-malformed/` | C-E8-01 |
| T-V4B4-15 | @case-machine | Given `envfacts-tool-only/` (ENV-FACTS row `node scripts/check-gh.mjs` ABSENT, `tool: gh`; CONTRACT cases `node tests/x.mjs` and `gh pr list`); when `contract --dir <fx>`; then the `node …` case is NOT flagged, the `gh …` case fails `C-W2-03` naming `gh`, and the message carries `C-E6-03` (G-12); given the same file without a `tool` column; then only `node` (argv[0]) is blacklisted — never `scripts/check-gh.mjs` | `node tests/v4/core/envfacts-tool-only.mjs` | `tests/fixtures/v4/envfacts-tool-only/` | C-E6-03 |
| T-V4B4-16 | @case-machine | Regression: given the v3 preflight fixtures; when `node tests/v3/preflight-tiering.mjs` and `node tests/v3/probe-timeout.mjs`; then both exit 0 as at 3.0.1, and the emitted ENV-FACTS.md header now carries the fifth `tool` column with the probe count unchanged (6 / 9) | `node tests/v4/core/envfacts-tool-only.mjs` | `tests/fixtures/v3/preflight-s/`, `tests/fixtures/v3/preflight-ml/`, `tests/fixtures/v3/slow-probe/` | C-E6-03 |

---

## Epic V4B5 — Harness: AMD-4, AMD-5, polarity, `tests/v4/core/*` wiring, fixture index, dogfood fixture + mechanical dogfood

Branch: `epic/v4b-harness` (worktree off `main`)
Depends on: V4B1–V4B4 (it proves them); SQ-A's three files for the live 11/11 check (W2 — that single case is IMPLEMENTED-NOT-VERIFIED with the reason until then).
Covers: C-E9-09, C-E11-05, C-E11-06 (+ the fixture index every V4B1–V4B4 `run:` cell relies on)
Scope: PRD D-B13 (harness half: AMD-4 gate count, AMD-5 eleven pairs, polarity, v4 section), D-B14 (fixture READMEs, `dogfood-project/`, `dogfood-run.mjs`), D-B15 (case ID present in every new failure message).

### Task checklist
- [ ] `tests/run-contract.mjs:108–116` (`T-E1-05`) — rewrite per AMD-4: load `tests/fixtures/v2/machine.v2.fc6abc8.json`, assert exactly 3 gate states there (`G1, G2, G3`); assert the live machine carries `G1, G2, G3`; assert every live state with `meta.gate` has `meta.human === true`. Comment cites AMD-4 in `delivery/decisions.md`. [D-B13]
- [ ] `tests/run-contract.mjs:292` (`T-E5-02`) — 11 pairs: add `scripts/build-report.mjs`, `scripts/report-template.html`, `assets/brand/default.brand.json` ↔ their `plugins/plan-it/skills/plan-it/` copies; `gate-check.mjs:1556–1565` `MIRROR_PAIRS` — the same 11 (AMD-5; the "8 pairs" comment updated). [D-B13]
- [ ] `tests/run-contract.mjs:284` (`T-E5-01`) — version literal `4.0.0`, landed in the W2 merge together with SQ-C's bump (PRD §9 item 7). [D-B13]
- [ ] `tests/run-contract.mjs:497–513` — v3 loop generalised over `parseAllContractCases()`: per-source sections (`-- v3 …`, `-- v4 (from delivery/v4/CONTRACT.md) --`), **polarity** (`^node tests/` ⇒ positive, exit 0 = PASS; `gate-check.mjs <verb>` ⇒ negative, non-zero = PASS; `manual:` skipped and listed; a row whose description contains `(positive)` is positive); PENDING via `mechanismGap` unchanged; computed totals per section; exit 1 on any regression in any section. [D-B13]
- [ ] `tests/v4/core/amd4-gate-count.mjs`, `mirror-pairs-11.mjs`, `run-contract-polarity.mjs`, `fixture-index.mjs`, `case-id-in-source.mjs`, `dogfood-run.mjs` — new scripts. [D-B13–D-B15]
- [ ] `tests/fixtures/v4/README.md` — index table `| fixture | case(s) | row |` for every `tests/fixtures/v4/*` entry (v3 convention); each fixture dir carries its own `README.md`. [D-B14]
- [ ] `tests/fixtures/v4/dogfood-project/` — `.plan-it/dogfood.state.json` (autonomous-draft, `run.deliveryRoot: "delivery/"`, history through `handoff`, G0/G1/G2/G3/G4 recorded, `gates.G2.defaults` non-empty, `contract.version: "1.0"`, `render.outputs[]` with hashes), step variants `.plan-it/steps/<nn>-<state>.state.json` for every autonomous-draft edge, `delivery/{CONTRACT.md (v1.0 + a `CONTRACT.draft.md` v1.0-draft twin for the draft freeze step), GLOSSARY.md, KICKOFF.md, DECISIONS.md, STATUS.md (Disposition column + Residuals + Log), PLAN-REVIEW.md (Reviewed-by line), TEST-CONTRACT-REVIEW.md, SCOPE-BRIEF.md, prds/prd-1.md, epics/epics-1.md (Tier Table + ≥1 case row + cascade waivers)}` and a stamped `.html` twin beside every family-kind md (rendered by SQ-A's `build-report.mjs` when present in W2, else stamped by the V4B4 helper). [D-B14]
- [ ] `tests/v4/core/dogfood-run.mjs` — copy the fixture to a temp dir; for each step variant run `state --dir <tmp> --run dogfood` (exit 0, expected state and next events); `freeze --draft <tmp>/delivery/CONTRACT.draft.md --dir <tmp> --run dogfood` exit 0; `freeze <tmp>/delivery/CONTRACT.md --dir <tmp> --run dogfood` exit 0; `handoff <tmp>/delivery --dir <tmp> --run dogfood` exit 0 with `glossary` and `mirror --require-html` steps reported; `mirror --dir <tmp>/delivery --require-html` exit 0; `glossary <tmp>/delivery` exit 0; `archive dogfood --dir <tmp>` exit 0 and `runs --dir <tmp>` shows `1 archived`. Negative half: delete `GLOSSARY.md` ⇒ `handoff` exit 1 (`C-E10-02`); flip one byte in `KICKOFF.md` ⇒ `handoff` exit 1 `MIRROR_STALE`. [D-B14]
- [ ] Every `fail(...)` string added by V4B2–V4B4 is checked by `case-id-in-source.mjs`: for each `@case-machine`/`@case-guard` CONTRACT row whose `run:` is a gate-check invocation, `gate-check.mjs` source contains the row ID (the `mechanismGap` readiness signal, F-B17). [D-B15]
- [ ] Mirror sync of `gate-check.mjs` (`MIRROR_PAIRS`) plugin → root; `mirror-check` exit 0 (8/8 pre-W2, 11/11 post-W2) before merge.

### Tier Table

| tier | effort | escalation | scaffold-pointer |
|---|---|---|---|
| top | adversarial verify of the whole lane against the merged tree (judgment slice) | already top; coordinator re-runs a failed slice, recorded | build-it:iteration-impl#slice=V4B5-verify |
| mid | spec'd-implementation vs this Test Contract (AMD-4/AMD-5 edits, polarity, dogfood script) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B5 |
| low | mechanical (fixture READMEs, dogfood fixture files, step variants) | escalate-on-struggle-one-tier-up | build-it:iteration-impl#slice=V4B5-fixtures |

Cascade map: partial-failure — T-V4B5-06 (a run-contract execution where one section has a PENDING row and another a FAIL: each section reports its own computed totals; the exit code reflects the failure, the pending row is never folded into either); rollback/compensation — N/A (waiver: `rollback/compensation: N/A — the harness only reads and executes; nothing to undo`); failed-recovery→escalation — N/A (waiver: `failed-recovery→escalation: N/A — asserted by V4B2 T-V4B2-11 and V4B4 T-V4B4-07; the harness has no retry loop of its own`); recovery/resume — T-V4B5-08 (the dogfood resumes at every recorded step and reaches `handoff`, then is archived); adversarial-verify — T-V4B5-03 (pair lists are compared computed-from-source, not typed) and T-V4B5-09 (a tampered twin in the dogfood is caught by `handoff`).

### Test Contract — V4B5 harness + dogfood   (BINDING: 100% pass or /iterate)
Types: [unit][integration][e2e-offline] · Count: 12 · [REAL]: 0 · Surfaces: node harness scripts over the repo's own files and fixtures, offline
DoD = every case below PASS (T-V4B5-01 … T-V4B5-12). No [REAL] case is VERIFIED on a mock.

| ID | @tag | Case (given / when / then → expected) | run: | fixture | Covers |
|---|---|---|---|---|---|
| T-V4B5-01 | @case-machine | Given the rewritten `T-E1-05` logic; when applied to the v2 pin and the live machine; then the pin has exactly 3 gate states `G1, G2, G3`, the live machine contains those three, and every live `meta.gate` state (5 in 4.0.0) has `meta.human === true` (AMD-4) | `node tests/v4/core/amd4-gate-count.mjs` | `tests/fixtures/v2/machine.v2.fc6abc8.json`, live `machine.json` | C-E11-05 |
| T-V4B5-02 | @case-machine | Given a temp machine derived from the live one with `planReview.meta.human` removed, and another with `G2` dropped; when the AMD-4 check runs; then each fails naming the gate (`not marked human` / `missing G2`) — the amended case still has teeth | `node tests/v4/core/amd4-gate-count.mjs` | temp variants of live `machine.json` | C-E11-05 |
| T-V4B5-03 | @case-packaging | Given `gate-check.mjs` and `tests/run-contract.mjs` sources; when `MIRROR_PAIRS` and the `T-E5-02` pair list are parsed (computed, not typed); then both have exactly 11 entries, are equal as sets, and include `scripts/build-report.mjs`, `scripts/report-template.html`, `assets/brand/default.brand.json` (AMD-5, G-14) | `node tests/v4/core/mirror-pairs-11.mjs` | live sources | C-E9-09 |
| T-V4B5-04 | @case-packaging | Given the merged tree after SQ-A lands (W2); when `gate-check mirror-check`; then exit 0 and `PASS — mirror-check: 11 pair(s) byte-identical`; until W2 this case is IMPLEMENTED-NOT-VERIFIED with reason `SQ-A files absent`, never a fake green | `node plugins/plan-it/skills/plan-it/scripts/gate-check.mjs mirror-check` | live tree (positive) | C-E9-09 |
| T-V4B5-05 | @case-machine | Given the polarity function with rows `node tests/v4/core/x.mjs`, `node plugins/…/gate-check.mjs reconcile --dir …`, `manual: …`, and a gate-check row described `(positive)`; when classified; then positive / negative / skipped / positive respectively; a fake gate-check row that exits 0 against its violating fixture is reported FAIL `fail-closed broken`, a positive script exiting 1 is FAIL, a positive script exiting 0 is PASS | `node tests/v4/core/run-contract-polarity.mjs` | inline rows + stub scripts in a temp dir | C-E9-10 |
| T-V4B5-06 | @case-machine | Given a temp CONTRACT with one row whose script is missing (PENDING), one negative row (fail-closed OK) and one positive row that fails; when the run-contract section logic runs over it; then output shows `PEND`, `PASS`, `FAIL` on the right rows, computed totals `1/2 … 1 pending`, exit 1 — the pending row is never counted into pass or fail (partial result, honest) | `node tests/v4/core/run-contract-polarity.mjs` | temp CONTRACT + stub scripts | C-E9-10 |
| T-V4B5-07 | @case-machine | Given `tests/fixtures/v4/`; when every directory is listed; then each has a `README.md` naming ≥1 `T-V4B` or `C-E` ID, `tests/fixtures/v4/README.md` indexes every directory (computed set equality), and every path named in a `@case-machine`/`@case-guard` `run:` cell of `delivery/v4/CONTRACT.md` exists on disk | `node tests/v4/core/fixture-index.mjs` | `tests/fixtures/v4/**`, `delivery/v4/CONTRACT.md` | C-E11-06 |
| T-V4B5-08 | @case-packaging | Given `dogfood-project/` copied to a temp dir; when every step variant is driven through `state --run dogfood`, then `freeze --draft`, `freeze`, `handoff` (embedded glossary + mirror `--require-html`), `mirror --dir --require-html`, `glossary`, `archive dogfood`, `runs`; then every exit is 0, `handoff` output lists the glossary and mirror steps, `archive` moves the run to `.plan-it/done/` and `runs` counts `1 archived` — the run resumes at each step and reaches `handoff` in autonomous-draft mode with every twin fresh | `node tests/v4/core/dogfood-run.mjs` | `tests/fixtures/v4/dogfood-project/` | C-E11-06 |
| T-V4B5-09 | @case-packaging | Given the same temp copy; when `GLOSSARY.md` is deleted; then `handoff` exits 1 naming `C-E10-02`; when one byte of `KICKOFF.md` is flipped (tampered source); then `handoff` exits 1 with `MIRROR_STALE` and the twin name; when the state's `contract.version` is set to `1.0-draft` at `handoff`; then `state --run dogfood` exits 1 `draft contract cannot hand off` — the dogfood fails closed on each | `node tests/v4/core/dogfood-run.mjs` | `tests/fixtures/v4/dogfood-project/` | C-E11-06 |
| T-V4B5-10 | @case-machine | Regression: given the v3 CONTRACT; when `node tests/v3/fail-closed-sweep.mjs`; then exit 0 with `25` rows mechanism-ready and fail-closed (the `parseContractCases()` default is unchanged by the lib edits) | `node tests/v4/core/run-contract-polarity.mjs` | `delivery/v3/CONTRACT.md`, `tests/fixtures/v3/**` | C-E9-10 |
| T-V4B5-11 | @case-machine | Given the merged lane (post-W2 for the version literal); when `node tests/run-contract.mjs`; then the v2 section is 51/51, the v3 section has 0 FAIL, the v4 section has 0 FAIL and its totals are computed from `delivery/v4/CONTRACT.md`, exit 0; pre-W2 the only permitted red lines are `T-E5-01` (version) and `T-E5-02` (SQ-A pairs), recorded as IMPLEMENTED-NOT-VERIFIED with those reasons | `node tests/run-contract.mjs` | live tree (positive) | C-E9-10 |
| T-V4B5-12 | @case-machine | Given `delivery/v4/CONTRACT.md` and `gate-check.mjs`; when every `@case-machine`/`@case-guard` row whose `run:` invokes `gate-check.mjs` is checked; then its ID appears in the gate-check source (so `mechanismGap` reports it ready, never PENDING once the verb exists) — the list of missing IDs is printed and must be empty | `node tests/v4/core/case-id-in-source.mjs` | live sources | C-E9-10 |

---

## Failure-state coverage (adversary gate D-B1 — every declared failure state this lane touches is an asserted outcome)

| Failure / recovery state (CONTRACT §3) | Asserted by |
|---|---|
| CLOSED_WITHOUT_PLAN (REJECTED without memo; accepted with memo) | T-V4B2-02, T-V4B2-03 |
| REJECTED (`state` verb refusals) | T-V4B2-01, -02, -04, -06, -09, -10, -11 |
| REVIEW_CONTRADICTED (recovery edge into AMENDMENT) | T-V4B1-14, T-V4B2-11 |
| ESCALATED (second contradiction; still-stale twin after re-render) | T-V4B2-11, T-V4B4-07 |
| WRITE_DENIED (guard) | T-V4B3-13, T-V4B3-14 |
| ARCHIVE_REFUSED | T-V4B3-09, T-V4B3-10 |
| MIRROR_STALE | T-V4B4-02, T-V4B4-05, T-V4B4-07, T-V4B5-09 |
| MIRROR_REJECTED / HTML_UNSTAMPED | T-V4B4-03, T-V4B4-04 |

## CONTRACT coverage — every C-E row in the SQ-B lane → T-cases (no silent drops)

| CONTRACT row | @tag | Binding case(s) | Epic |
|---|---|---|---|
| C-E2-05 | @case-machine | T-V4B4-06 | V4B4 |
| C-E2-06 | @case-machine | T-V4B4-01, T-V4B4-02, T-V4B4-05 | V4B4 |
| C-E2-07 | @case-machine | T-V4B4-03, T-V4B4-04, T-V4B4-07 | V4B4 |
| C-E6-01 | @case-machine | T-V4B2-01, T-V4B2-02 | V4B2 |
| C-E6-02 | @case-machine | T-V4B2-03 | V4B2 |
| C-E6-03 | @case-machine | T-V4B4-15, T-V4B4-16 | V4B4 |
| C-E7-01 | @case-machine | T-V4B1-01, T-V4B1-02, T-V4B1-03, T-V4B1-04, T-V4B1-14 | V4B1 |
| C-E7-02 | @case-machine | T-V4B1-07 … T-V4B1-12 (six negatives, one each) | V4B1 |
| C-E7-03 | @case-machine | T-V4B2-06, T-V4B2-07, T-V4B2-08 | V4B2 |
| C-E7-04 | @case-machine | T-V4B2-09 | V4B2 |
| C-E7-05 | @case-machine | T-V4B2-13, T-V4B2-14 | V4B2 |
| C-E7-06 | @case-machine | T-V4B2-10, T-V4B2-11 | V4B2 |
| C-E7-07 | @case-machine | T-V4B2-04, T-V4B2-05 | V4B2 |
| C-E8-01 | @case-machine | T-V4B4-11, T-V4B4-14 | V4B4 |
| C-E8-02 | @case-machine | T-V4B4-12 | V4B4 |
| C-E8-03 | @case-machine | T-V4B4-13 | V4B4 |
| C-E9-01 | @case-machine | T-V4B2-12 | V4B2 |
| C-E9-02 | @case-machine | T-V4B3-01, T-V4B3-02, T-V4B3-03 | V4B3 |
| C-E9-03 | @case-machine | T-V4B3-04, T-V4B3-05, T-V4B3-06, T-V4B3-07 | V4B3 |
| C-E9-04 | @case-machine | T-V4B3-08 | V4B3 |
| C-E9-05 | @case-machine | T-V4B3-09, T-V4B3-10 | V4B3 |
| C-E9-06 | @case-machine | T-V4B3-11, T-V4B3-12 | V4B3 |
| C-E9-07 | @case-guard | T-V4B3-13, T-V4B3-15 | V4B3 |
| C-E9-08 | @case-guard | T-V4B3-14 | V4B3 |
| C-E9-09 | @case-packaging | T-V4B5-03, T-V4B5-04 (the row's own `run:` script `tests/v3/mirror-wired-into-release.mjs` is v3-bound and unowned — PRD §9 item 3) | V4B5 |
| C-E9-10 | @case-machine | T-V4B3-16, T-V4B3-17, T-V4B5-05, T-V4B5-06, T-V4B5-10, T-V4B5-11, T-V4B5-12 | V4B3, V4B5 |
| C-E10-01 | @case-machine | T-V4B4-08, T-V4B4-09 | V4B4 |
| C-E10-02 | @case-machine | T-V4B4-10 | V4B4 |
| C-E11-04 | @case-machine | T-V4B1-05, T-V4B1-06, T-V4B1-13, T-V4B2-15 | V4B1, V4B2 |
| C-E11-05 | @case-machine | T-V4B5-01, T-V4B5-02 | V4B5 |
| C-E11-06 | @case-packaging | T-V4B5-07, T-V4B5-08, T-V4B5-09 (mechanical half; the model-driven half is QA W3 — PRD §9 item 11) | V4B5 |

31/31 SQ-B CONTRACT rows mapped; 0 uncovered; 0 silent drops. Two rows bind with an explicit dependency note rather than a silent assumption: C-E9-09 (live 11/11 needs SQ-A's files, W2) and C-E11-06 (model-driven dogfood is QA's).

Totals (counted from the five tables above, never estimated): V4B1 14 · V4B2 15 · V4B3 17 · V4B4 16 · V4B5 12 — **74 binding cases, 0 `[REAL]`**. Re-derive with `grep -c '^| T-V4B' delivery/v4/epics/epics-b-core.md` (expect 74 rows in the five Test Contract tables; the coverage table above cites IDs in prose cells, not as row IDs).

## Corrections flagged for the orchestrator

Full text in `delivery/v4/prds/prd-b-core.md` §9 (thirteen items). The ones that change other lanes' work or the build order: (1) `CLOSED_WITHOUT_PLAN` vs `closedWithoutPlan` state naming — an AMD line recommended; (2) SQ-B fixture ownership glob too narrow for the `run:` cells the CONTRACT itself names; (3) `tests/v3/mirror-wired-into-release.mjs` is v3-bound and unowned — C-E9-09's own `run:` cannot pass without an owner updating it for 4.0.0; (4) `tests/v3/kickoff-pinning.mjs:38–45, 54` (SQ-C) must widen its `.plan-it/state.json` literal when templates move to `<slug>.state.json`; (5) `run-contract`/`fail-closed-sweep` polarity is inverted for harness-script rows — V4B5 fixes run-contract, the sweep needs an owner; (6) the 3.0.1 epic-heading grammar cannot see `V4B1` headings — every squad should write `## Epic V4x<n> —` until V4B3 merges; (7) version literal `4.0.0` in `run-contract.mjs:284` flips in the W2 merge with SQ-C; (8) ENV-FACTS optional `tool` column needs a line in `references/formats.md` §9 (SQ-C); (9) byte-pin W0/V4B1 overlap resolved as verify-then-create; (10) this run's own state file should be renamed to `.plan-it/v4.state.json` after V4B3 merges; (11) C-E11-06's model-driven half belongs to QA W3; (12) `gates.G2.pendingReview` should be added to CONTRACT §4.6; (13) SQ-C prose should cite the verb names of CONTRACT §4.5 as final. Build order inside the lane: V4B1 → V4B2 → V4B3 → V4B4 → V4B5; V4B3 before QA.

Status of all five epics: NOT-STARTED.
