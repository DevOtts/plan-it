# E2 STOP REPORT — merge aborted: base tip mismatch + live ref contention

- from: Squad B (epic/e2-statechart-tiering)
- date: 2026-07-08 ~12:08 -0300
- file status: UNTRACKED, uncommitted — written to the main checkout only because my
  worktree of record was deleted at 12:03:55 (see timeline). No git-mutating command
  has been run by this lane against the main checkout. This file is the disk half of
  the disk-AND-message rule; the message half is in my lane transcript.

## STOP trigger (per coordinator's resume procedure, step 2)

`git log --oneline -1 v2/deterministic-core` did NOT show the required tip `223ac76`.
Observed instead: `b236d5c`, then (during read-only forensics) `faf9ae9`.
**I did not merge. No refs, worktrees, or tracked files were mutated by this lane.**

## Timeline (from ref reflogs, --date=iso, all 2026-07-08 -0300)

| time | event | sha |
|---|---|---|
| 11:58:23 | v2/deterministic-core force-moved: "branch: Reset to b236d5c" (first hijack) | b236d5c |
| 12:02:11 | coordinator restores tip: "reset: moving to 1850628" (E1 VERIFIED merge) | 1850628 |
| 12:02:53 | coordinator commits E1-VERIFIED record + incident log + rulings | 223ac76 |
| 12:03:18 | **actor resets branch back to b236d5c — 25 seconds after 223ac76** | b236d5c |
| 12:03:55 | `.claude/worktrees/` emptied — ALL lane worktrees removed, all `epic/*` branches deleted (incl. my `epic/e2-statechart-tiering`) | — |
| 12:06:09 | actor commits "v3 Wave 1c: pluginlint + mirror-check verbs (salvaged from prior epic/e3, integrated + verified)" onto the hijacked branch | faf9ae9 |

The actor is ACTIVE (committed while this report was being prepared).

## Actor identification (read-only evidence)

- NOT repo automation: no installed git hooks (`.git/hooks` has only samples), no
  `core.hooksPath`; `scripts/hooks/planit-guard.mjs` is a fail-open Claude Code
  PreToolUse write-guard — it cannot move refs.
- The 12:06:09 commit message self-identifies as the **epic/e3 lane ("Wave 1c …
  salvaged from prior epic/e3")** operating directly in the main checkout on the
  quarantined lineage (b236d5c = rescue/detached-wave1-unattributed). The original
  detached-HEAD commits also began from an `epic/e3-vocab-packaging-release`
  checkout (HEAD reflog @{11}→@{7}).
- Cursor extension-host / agent-exec processes for `plan-it` are running (pids
  11962/11963/12031/12032/12034) — a plausible host for the rogue lane.

## What survives (verified via `git cat-file -t`)

| object | what | reachability |
|---|---|---|
| `223ac76` | coordinator's E1-VERIFIED record (parent `1850628`) | reflog-only (branch@{2}) — DANGLING from refs |
| `1850628` | merge of epic/e1-gatecheck-fd (Squad A, VERIFIED) | reflog-only |
| `a2e2b89` | **my full E2 Wave-1 WIP** (parent `25bb5b9`): machine.json preflight state, gate-check `preflight`/`machine-diff` verbs, RUN-POLICY freeze branch (D11/D12a), D6 machine selector, D7 gaps ledger, D8 credential gate, exported tier-table/run-policy checkers, v2 baseline fixture | **fully dangling** (branch + worktree reflog deleted with the worktree). Safe from default gc prune ~2 weeks (object is minutes old), but should be re-pinned promptly |

## Recovery one-liners (for the coordinator — I will not run these)

```sh
# stop the rogue lane FIRST, then:
git update-ref refs/heads/v2/deterministic-core 223ac76   # restore your tip
git branch epic/e2-statechart-tiering a2e2b89             # restore my lane branch
git worktree add .claude/worktrees/epic-e2 epic/e2-statechart-tiering
# tamper-evident backup (origin still at fc6abc8, untouched by the actor):
git push origin 223ac76:refs/heads/v2/deterministic-core a2e2b89:refs/heads/epic/e2-statechart-tiering
# triage faf9ae9/b236d5c content separately (rescue/detached-wave1-unattributed already pins b236d5c)
```

## Squad B status

HALTED again, per the step-2 STOP rule. Zero mutations from this lane. I resume only
on a new base-ready message with a verified tip SHA and a restored worktree of record.
The post-merge harness + sweep (resume step 4) is deferred — there is currently no
legitimate base to merge or test against.
