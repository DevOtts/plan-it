# Findings F: mcp-tparty (plan→exec) & migrate-master-admin (plan→2 execs)

## Pair: done-mcp-tparty-plan → mcp-tparty-execution
### Verdict (1 line): B+ — thorough 6-stream discovery + frozen CONTRACT v1.1 carried cleanly into execution, but a gap the plan's own research flagged (composer "installed MCPs" listing) resurfaced as the user-blocking failure, and [REAL] runtime verification was under-provisioned.

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | Execution launched strictly from the frozen package and honored plan gotchas verbatim (nested verb shape, enum guard, wave order). | Exec USER [8]: "Build the third-party MCP integrations program from the frozen spec package at docs/implementation/mcp-third-party-integrations/ (planned via /plan-it, Shape 3, size L, CONTRACT v1.1 frozen)"; "Do NOT churn the auth_mode enum (T-P1-08 fails the build if a 4th value is added)"; "Verb shape is NESTED (integrations/:id/connections). Do not reintroduce the flat path." |
| 1 | Plan fidelity (deviation) | Wave-1 exit proof ("brain-agent chat sees {airtable}__{tool} BaseTools") was implemented but not machine-verified end-to-end; coordinator carried "IMPLEMENTED-NOT-VERIFIED" risk on unreachable [REAL] targets and the user ended up testing manually. | Exec tail: "Test Contract T-P2-01..14, same flag gate, IMPLEMENTED-NOT-VERIFIED risk for unreachable [REAL] targets"; USER [58]: "let me test myself in https://admin-praxya.usengine.io/chat". |
| 2 | Late discoveries | Composer `/` picker showed no MCPs at runtime; trace agents discovered mention→tool resolution is server-side only: "no `required_mcp_tools`, `AppTokenGuard`, or `app-api` references exist anywhere in the brain-agent Python service". Also live-test infrastructure surprises (VPS Mongo auth on 167.86.98.85:27027, MONGODB_URI wrangling) not provisioned by the plan. | Exec USER [59] + task a11080732acb0e601 result: "A grep for `required_mcp_tools`/`mcpTools` finds nothing in the send path". |
| 2 | Late discoveries (plan HAD the signal) | Plan's Stream C research explicitly flagged the exact gap execution hit — but it never became an epic task: "no existing 'installed MCPs per tenant' list endpoint (closest analog `listIntegrations()` is called without the `installed` filter — worth Stream A confirming)". | read-chat-done-mcp-tparty-plan.md USER [6] (task abf7ac6b15538820a, stream-C-composer.md). |
| 3 | Test Contract health | Contracts were live and enforced structurally (T-P1-08 build-failing enum guard is a strong pattern); but several [REAL] cases were unreachable in-session, producing "IMPLEMENTED-NOT-VERIFIED" instead of red/green — done-claims leaned on adversarial re-verification rather than contract pass. | Exec tail: "I'll run the same adversarial verification when it reports back"; spec files `src/integrations/mcp-schema.spec.ts`, `src/integration-connections/mcp-connections.integration.spec.ts`. |
| 4 | Human friction | 2 substantive interventions: USER [58] (had to take over testing) and USER [59]: "I don't see any MCP (airtable, google ads neither meta ads) listed in the smart composer. there for I'm not able to test the MCP in the chat." Plus close-out chores (USER [11] "/next-session-prompt", USER [12] HTML visual + debrief + skill visibility). | mcp-tparty-execution.md USER [58]–[62]. |
| 5 | Rework | Two trace subagents (a11080732acb0e601, a2a441a22ed68b57f) spent ~40k+ tokens each re-deriving composer/agent mention plumbing that plan Streams C/D had already researched — re-discovery, not new build. | Exec USER [61]/[62] task-notifications, `<subagent_tokens>40862`. |
| 6 | Cross-session handoff | Smooth: exec step 0 was `/read-chat "done-mcp-tparty-plan"`, plus a doc reading order with conflict-precedence rules ("On any DESIGN conflict, architecture.md WINS… the CONTRACT (Part 1) WINS"). No lost artifacts. | Exec USER [8] READ FIRST list. |
| 7 | Wins to keep | (a) Frozen CONTRACT v1.1 + precedence rules; (b) gotcha carry-over into the launch prompt ("McpToolFactory owns collision-guarding — ToolRegistry.register() overwrites-with-warning (base.py:117-120), it does NOT reject duplicates"); (c) research streams produced file-precise touch-maps (`McpPanel.tsx` wired into `ConsumerDetailPage.tsx`); (d) build-failing contract case (T-P1-08). | Plan USER [3]–[8] stream reports; exec USER [11] fable-it args. |

### Enhancement candidates (max 5 per pair):
- **EC-F1: Research-gap ledger** — gaps flagged inside research stream reports (composer `installed` filter, per-tenant encryption key, Facebook 4-client allowlist) never traced into epics; exec hit the flagged composer gap as the sole user-blocking failure → GAPS.md where every flagged gap must terminate in an epic task, contract case, or explicit deferral, gate-checked at freeze → agile-split/contract-freeze phase.
- **EC-F2: [REAL]-case runtime preflight** — [REAL] cases needed live creds/endpoints (VPS Mongo 167.86.98.85:27027, tenant login, LLM keys) discovered mid-execution → each [REAL] case carries a "runtime prerequisites" block verified reachable during pre-grounding → test-contract phase.
- **EC-F3: User-facing wave exit proof** — Wave-1 exit criterion was agent-internal, so the user's own smoke test (composer UI) failed despite wave green → every wave exit must include ≥1 proof executable by the human from the product UI → epic/wave definition phase.
- **EC-F4: IMPLEMENTED-NOT-VERIFIED as first-class state** — coordinator improvised this status; v3 should define it in machine.json with the rule that it can never satisfy DoD (must convert to PASS or WAIVED-with-reason) → statechart.

## Pair: migrate-master-admin-plan → migrate-master-admin-execute-loudr + migrate-master-admin-execute-claudott
### Verdict (1 line): A- — exceptional package (21-case/14-[REAL] contract, statechart, gate-check.mjs, live-cross-checked epics) that execution cited constantly; docked for a frozen-contract semantic error shipped live (§2 `connectors_brain_api_url`), an unprocured credential gate (devotts Vercel token), and a missed CORS/origin surface.

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | Both executions followed wave order, merge order (E1→E2), gating and pre-change-capture rules; loudr's builders refused out-of-plan actions ("Both PRs open, CI green on master-admin, not merged (per instructions)"; declined `workflow_dispatch` as "a real side effect outside what I was authorized to do"). | loudr USER [7] e2-builder report; USER [5]: "CONTRACT.md (v1.1 — THE LAW; statechart §3 governs cutover order)… no [REAL] test case passes on a mock". |
| 1 | Plan fidelity (deviation) | claudott executed E4 despite launch-prompt "HARD STOP before E4" — but only on the user's live bug report as go-signal; CONTRACT amended twice in-flight (v1.2, v1.3). | claudott ASSISTANT [247]: "E4 ADDENDUM — live repoint EXECUTED (2026-07-05 …, on your go)… Trigger: your report that praxya's Add-Integration still fetched from global-admin-backend.vercel.app". |
| 2 | Late discoveries | (a) ungated `wait-db` init container (`until nc -z master-admin-db 5432`) found by E1 verifier, not planning; (b) E3 `ALLOWED_ORIGINS` omitted tenant admin origins → live CORS failure; (c) E4-runbook line 35 "rsync E1's merged zone-agent `dist/` → box" encoded a deploy path that never ships; (d) Supabase envs "exist only in the Vercel dashboard" (caught in discovery) but the devotts token never procured pre-execution. | claudott ASSISTANT [33], [51], USER [24] curl repro vs https://master-admin-api.usengine.io/praxya/integrations; plan USER [10] Stream A. |
| 3 | Test Contract health | Contract actively used: "E1 CONTRACT: PASS (4/4 cases pass; diff clean; discrepancy CONFIRMED)"; haiku runner reported per-check states. One case untestable as scripted: "CHECK 3 — T-E2-C1 GHCR frontend image URL baked: BLOCKED … docker pull returns 403 Forbid[den]" — accepted 3/4 with rationale. | claudott USER [10], USER [13]. |
| 4 | Human friction | 4 interventions: (1) claudott USER [4] resume + model-tiering order; (2) USER [8]: "Sorry but there is no loudragency tenant in engine. you are mistaken cause I was using previously another claude subscription" + hand-pasted session log; (3) USER [24] CORS bug + curl; (4) USER [31] "update the @.secrets/.full.credentials as we don't need to kep the old vercel from andre2654s". #3/#4 trace to plan gaps. | claudott USER [4], [8], [24], [31]. |
| 5 | Rework | "CONTRACT §2 had a spec error: `connectors_brain_api_url` is the tenant's *own* api-core URL, not master-admin. E1 had faithfully written the wrong value into the repo — fixed in processor **#28**"; drift gate nearly reverted pinned `sha-10ecb40` to `:latest` (**#30**); wait-db fast-follow PR; E4-runbook rewritten. Faithful execution of a wrong contract = plan-attributable rework. | claudott ASSISTANT [250] items 1–2, [38]–[46], [51]–[55]. |
| 6 | Cross-session handoff | File package handed off perfectly (claudott re-grounded from KICKOFF/CONTRACT/STATUS/decisions.md alone), but *session-level* handoff across subscriptions failed: read-chat matched a wrong "Loudr Migration" transcript in `-Users-macbook-Workspace-8FA`, the assistant asked about a hallucinated "loudragency tenant" (AskUserQuestion), and the user interrupted ("[Request interrupted by user for tool use]") and pasted the loudr log manually. | claudott ASSISTANT [3]–[14], USER [7]–[8]. |
| 7 | Wins to keep | (a) `gate-check.mjs verify` after every stream/epic ("idle ≠ delivered"); (b) statechart states (DEPLOYED_SHADOW→SOURCE_REPOINTED→LIVE_REPOINTED→CONVERGED→DECOMMISSIONED) as the run coordinate system; (c) epic authors cross-checking live repo at plan time: "found the build-arg plumbing is fully absent today… Flagged that explicitly so the task descriptions say 'add' not 'verify'"; (d) per-task rollback keyed to T-E4-01 baseline capture; (e) `.taskstate/mavm-run/decisions.md` adjudication log; (f) launch-prompt "play back the non-negotiables" ritual. | plan USER [14]/[16]; claudott ASSISTANT [17]–[25], [246], [259]. |

**Divergence between the two executions of the same plan (ambiguity signal):**
- loudr ran Wave 0 only and died at "You've hit your session limit · resets 3am (America/Sao_Paulo)" with e1-builder output unreviewed; claudott merged E1/E2 and ran E3→E4 to CONVERGED with two contract amendments. Delta driver was external (usage limits) but exposed that the package defines no "resume from mid-wave" artifact.
- e1-builder escalated two judgment calls the epic under-specified, which different sessions could answer differently: "(1) T-E1-05/dist commit — literal epic vs. actual repo convention, and (2) whether HEALTH_POLL_ENABLED should've been added to engine's block despite not being in T-E1-07's explicit list" (loudr USER [8]).
- claudott initially reasoned from the *wrong* prior session — divergence caused purely by transcript-locating ambiguity across subscription config dirs (`.claude-loudr` vs `.claude` vs 8FA).

### Enhancement candidates (max 5 per pair):
- **EC-F5: Credential procurement gate at G2** — E3 was known-GATED on "my fresh devotts-scoped Vercel token" and Stream A knew Supabase values lived only in the Vercel dashboard, yet execution still stalled → plan-it emits a CREDENTIALS-NEEDED checklist in the batched human-decision gate so secrets are procured before /fable-it → human-gate (G2) phase.
- **EC-F6: Contract config-key semantics must cite consuming code** — CONTRACT §2 misdefined `connectors_brain_api_url`; lesson recorded verbatim: "config-key names lie (read the module line that consumes them)" → every contract config key requires a file:line citation of consuming code, verified at gate-check → contract-freeze phase.
- **EC-F7: Consumer-origin/CORS matrix for endpoint migrations** — occurrence sweep covered URL producers but not browser origins; `ALLOWED_ORIGINS` missed all 4 tenant admin origins → any URL-migration discovery must enumerate consuming origins alongside occurrences → discovery phase.
- **EC-F8: Mid-wave resume manifest** — loudr's limit-death left "e1-builder finished but output NOT yet reviewed" recoverable only via hand-pasted log after read-chat mis-matched across subscriptions → require a RESUME block in STATUS.md (statechart position + unreviewed artifacts + next command), making resume file-based and subscription-independent → package layout/KICKOFF phase.
- **EC-F9: Epic conventions & defaults block** — builders stalled on repo-convention ambiguities (dist-commit, HEALTH_POLL_ENABLED placement) → epics carry a conventions section auto-derived from pre-grounding so mechanical builders don't hold for coordinator calls → epic-authoring phase.

## TL;DR
- Both plans were followed faithfully; worst failures were *faithful execution of plan errors* (CONTRACT §2 wrong `connectors_brain_api_url` → live fix in processor #28), not deviation.
- Flagged gaps died in research docs — mcp-tparty's user-blocking composer gap was literally written in stream-C's plan-time report.
- Build-failing contract cases (T-P1-08) and [REAL]-on-no-mock rules worked; weak spots are unreachable [REAL] targets ("IMPLEMENTED-NOT-VERIFIED") and BLOCKED end-states (GHCR 403) with undefined semantics.
- Credentials were the #1 recurring blocker (devotts Vercel token, dashboard-only Supabase envs, VPS Mongo auth) — all knowable at plan time, none procured.
- Human interventions (2 + 4) mostly traced to plan gaps: UI testability, CORS origins, credential lifecycle, wrong-session context.
- Same plan diverged across two executions via session-limit death + cross-subscription transcript ambiguity (hallucinated "loudragency tenant"), plus two under-specified epic judgment calls (T-E1-05 dist; HEALTH_POLL_ENABLED).
- Keep for v3: gate-check "idle ≠ delivered", statechart-as-coordinate, epics cross-checked against live repo ("say 'add' not 'verify'"), per-task rollback baselines, decisions.md, read-first precedence order.
- CORS incident shows migrations owe behavior parity, not just URL replacement — origin surface belongs in discovery.
- Runbooks need the same live cross-check discipline epics got (E4-runbook's phantom `dist/` rsync).
- Mechanisms proposed: EC-F1 gap ledger, EC-F2 [REAL] preflight, EC-F3 user-facing wave proofs, EC-F4 verify-state semantics, EC-F5 credential gate, EC-F6 config-key citations, EC-F7 origin matrix, EC-F8 resume manifest, EC-F9 epic conventions block.
