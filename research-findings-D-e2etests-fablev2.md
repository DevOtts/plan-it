# Findings D: e2e-unit-tests (Engine QA System) + fable-v2 (fable-enhance-by-fable)

## Pair: e2e-unit-tests-plan → done-e2e-unit-tests-execution

- PLAN session: `49508533-a6ab-43c4-b101-32204af60fec` (project `-Users-macbook-Workspace-Engine-Engine-Core`, subscription `.claude-loudr`)
- EXEC session: `95656941-6fb1-437b-b786-918e2679f3d7` (same project, subscription `.claude-8ftools`)

### Verdict (1 line): B+ — plan artifacts (frozen CONTRACT, gate-check, per-epic Test Contracts) demonstrably drove an honest 7-epic ship, but the handoff broke across subscriptions and pre-grounding trusted a wrong claim about existing tests.

### Evidence table:
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | High. All 7 epics shipped against frozen CONTRACT v1.0; freeze gate enforced before squads ("Rule 1 — no squads until it passes"). Squad/model plan (haiku mechanical, sonnet judgment, fable crown jewel) was followed in exec via sonnet build agents in isolated worktrees. | "CONTRACT v1.0 frozen (gate-check PASS). Now the **tiered squad fan-out** — 5 squads writing PRDs + epics against the frozen contract"; exec: "🏁 Engine QA System — COMPLETE (all 7 epics shipped)" |
| 2 | Late discoveries | (a) Genuine DI bug on main: `src/common/graph/graph.service.ts:44-46` uses TS `?` without Nest's `@Optional()`, so `OntologyService` stays a hard dependency and `clientbible-projection.e2e.spec.ts` fails at `beforeAll`. (b) Broken `test:e2e` config on brain-api-core (T1–T5, T7) repaired mid-exec. (c) Stale `qa-e6-mongo`/`qa-e6-redis` container-start command → exit 125 port conflict vs native `mongod`. None were in the plan. | "repairing the broken `test:e2e` config (T1–T5, T7)"; "it's the stale `qa-e6-mongo`/`qa-e6-redis` container-start command (exit 125 = port conflict…)" |
| 3 | Test Contract health | Used and honest. E7: "12/12 Test Contract green, honest-closed"; adversarial controls verified to fail correctly ("SYN-1 → GAP, `fp9-9.99` → rejected. Corpus arithmetic 162+362+184=708 reconciled"). INV honesty cut both ways: "Docker was available, so 8 cases the epic pre-marked INV were actually run". "**TC-5 = documented pre-existing failure, not a regression.**" TC-8/TC-10 upgraded "from pure INV" with live evidence (gh run `28795330648`, `continue-on-error` lane). No fake-green observed. | plan: "Each epic ends in its own binding **Test Contract** (the meta-QA: tests that prove the QA system works — e.g. 'gate fails a PR with a deliberately broken spec')" |
| 4 | Human friction | 2 substantive mid-run interventions. PLAN USER[8]: "continue. analyse which subagent that can use lower models and change their models for us to save tokens. keep the main coordinator one as fable…". EXEC standing intent after crash: "computer restarted. let's ship the rest we need to do and merge what we need to do to complete this epic. I'll be out, so work autonomously." Plus close-out USER[24]: "great. let's /engine-session-debrief… then /exit-session". | quoted verbatim from digests |
| 5 | Rework | Modest but real: exec spent ~40 assistant turns (ASSISTANT [25]–[65]) debugging/patching the read-chat skill instead of QA work; plan also had to correct an inherited claim mid-run: "master-admin: NO CI gate, but correction — it DOES have a working `"test": "jest"` script in `backend/package.json` (contrary to shared-context's 'no test script yet' claim)". | edits to `~/.claude/skills/read-chat/distill_session.py` (`_roots()`, surfacing `config_dir`) |
| 6 | Cross-session handoff | FAILED initially. Exec opened with `/read-chat "e2e-unit-tests-plan"`; "The fuzzy match keeps landing on `db-organization-plan` — wrong session." Root cause: plan lived under `.claude-loudr`, exec under `.claude-8ftools`; distiller only searched `~/.claude/projects`. Exec had to make the skill "multi-subscription aware". Context compaction also hit (USER[25] continuation summary). | "Continuing: I'll check how the distiller discovers its projects root, then make the skill multi-subscription aware." |
| 7 | Wins to keep | `gate-check.mjs` (freeze gate PASS recorded), namespaced persisted run state (`d["state"]="freezeGate"`, event `COHERENCE_DONE`), frozen CONTRACT with dated amendment (D6 catalog-upkeep folded "into CONTRACT v1.1 as conductor (§10 AR-5 + §8 'shipped' catalog-append gate + dated changelog)"), binding per-epic Test Contracts with INV markings, one batched decision gate (AskUserQuestion "QA system only (Recommended)"). | quotes/paths above |

### Enhancement candidates (max 5 per pair):
- **EC-D1: Self-contained handoff manifest** — exec could not locate the plan session across subscriptions (`.claude-loudr` → `.claude-8ftools`; fuzzy match hit `db-organization-plan`) and burned ~40 turns patching `distill_session.py` → plan-it writes a repo-local handoff file (absolute doc paths, session_id, subscription config dir, launch prompt) so execution never depends on session-title lookup → handoff/kickoff phase.
- **EC-D2: Verify inherited claims during pre-grounding** — shared-context said master-admin had "no test script yet" but it "DOES have a working `"test": "jest"` script in `backend/package.json`" → pre-grounding must spot-check prior-doc claims against the repo (run the actual commands) before they enter the CONTRACT → discovery/pre-grounding phase.
- **EC-D3: Environment smoke-probe before Test Contract authoring** — exit-125 port conflict from stale `qa-e6-mongo`/`qa-e6-redis` start commands, broken `test:e2e` config, Docker availability flipping 8 INV cases to runnable → planning runs a scripted env probe (runners, ports, containers, CI triggers) and records results next to each case's INV/runnable marking → spec/contract phase.
- **EC-D4: Model-tier routing table as a first-class artifact** — Fernando had to interject "analyse which subagent that can use lower models… to save tokens. keep the main coordinator one as fable" → v3 emits a default per-squad model assignment (with escalate-on-struggle rule) at fan-out → agile-split/fan-out phase.
- **EC-D5: Crash/compaction resume protocol** — "computer restarted" + context compaction forced a human re-prompt to resume → persist a "next actions" cursor in run state (machine.json position already exists) so execution resumes unattended after restart/compaction → statechart/persistence.

## Pair: fable-v2-plan (fable-enhance-by-fable) → fable-v2-execution

- PLAN session: `d347f243-651c-47ea-af99-75b2cf756e02` (project `-Users-macbook-Workspace-Devotts-fable-it`, `.claude`)
- EXEC session: `046a3e33-c9e5-4de8-b1f6-5cf58a6cc285` (same project, same `.claude`)

### Verdict (1 line): A- — the frozen delivery package (KICKOFF read-order + CONTRACT v1.1 + 26-case contract) was consumed verbatim and the build shipped v2.0.0 with 26/26 evidence-backed in ~50 min (run window 2026-07-02 ~18:30 → ~19:20 UTC); only a packaging-time YAML bug and contract-size churn escaped planning.

### Evidence table:
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | Near-perfect. Exec launched with the plan's own KICKOFF prompt: "Before writing any code, read in order: 1. delivery/KICKOFF.md … 2. delivery/CONTRACT.md (v1.1 — the law…) 3. delivery/prd-fable-it-v2.md and delivery/epics-fable-it-v2.md (scope + the 26-case binding test contract) 4. docs/03-enhancement-spec.md". Final STATUS.md: "all epics merged to main, v2.0.0" with per-epic Test-contract column; PRs #5–#11. Locked decisions respected ("do not re-litigate locked decisions (KICKOFF list)… G2.1 hooks opt-in fail-open · G2.2 rebrand… G2.3 per-run run-memory"). | `/fable-it` command-args; memory: "fable-it v2 SHIPPED 2026-07-02 — all 7 epics merged (PRs #5–#11), 26/26 test contract, v2.0.0" |
| 2 | Late discoveries | Deploy-phase only: "an unquoted YAML frontmatter `description:` containing colon+space silently strips **all** skill metadata" (found inspecting "frontmatter bytes of failing skill" `plugins/fable-it/skills/chrome-cdp-control/SKILL.md`); lesson recorded in `.fable-it-reports/lessons.md`. Nothing during the build contradicted the plan. | exec [signal] lessons.md entry |
| 3 | Test Contract health | Strong. 26 tabletop-golden cases, "expected transcripts registered *before* implementation, judged by fresh-context agents, with live runs on Sonnet 5 and Opus 4.8"; hard rule: "never mark a test contract case passed without registered evidence"; DoD-2: "26-case test contract… passes 100% — tabletop-golden cases judged by a fresh-context agent". Evidence ledger kept (`.taskstate/evidence.md`: "DoD-1 … `gh pr list --state merged` → PASS"). Sizing churn during planning: package total edited 20 → 24 → 26 cases; KICKOFF re-edited to match. | plan Edit trail: "Package test-contract total: **20 cases**…" → "**24 cases**…" → KICKOFF "24-case" → "26-case" |
| 4 | Human friction | Zero mid-build interventions — fully autonomous between kickoff and "great job." Plan-phase intervention USER[9]: "another point that would be good if we can include, is the decision of which model to use in claude-teams/subagents… be cost aware is something that will be an amazing thing to have." Post-DoD additions USER[8]: "let's now deploy the new version of fable-it plugin. Make sure you update the readme…, create also a changelog file…, create a linkedin text post" + USER[10] "give me the full linkedin text". | quoted verbatim |
| 5 | Rework | Minimal. Contract totals rewritten twice during planning (20→24→26) rippling into KICKOFF.md edits; at close-out an old report archived/renamed (`report-2026-06-reports-folder-move.md`). No implementation thrown away. | plan Edits to `delivery/epics-fable-it-v2.md` + `delivery/KICKOFF.md`; exec `mv .fable-it-reports/report.md …` |
| 6 | Cross-session handoff | Clean — the model case. Same repo/subscription; plan wrote `delivery/KICKOFF.md` with a verbatim launch prompt plus memory files (`memory/MEMORY.md`, `memory/fable-it-v2-delivery-package.md`); exec read the package in prescribed order and played back "the one-line thing being built, the wave order, and the non-negotiables (gates not vibes…)" before starting. | plan ASSISTANT[68] Skill call: "Prepare the handoff for building fable-it v2 from the frozen delivery package at /Users/macbook/Workspace/Devotts/fable-it/delivery/ (CONTRACT v1.1, PRD, 7 epics with a 26-case binding test contract, KICKOFF.md already contains a draft launch prompt)" |
| 7 | Wins to keep | (a) Frozen CONTRACT with dated amendments: "# fable-it v2 — CONTRACT (frozen v1.0 · 2026-07-02)" amended to v1.1 after the cost-aware ask ("## 8. Changelog — v1.0 (2026-07-02) — frozen at Gate G3" + amendment). (b) KICKOFF verbatim launch prompt. (c) Evidence ledger + fresh-context verifier ("Fresh-context verifier: final report… no access to the implementation conversation and must not seek it"). (d) Batched G1 scope gate: AskUserQuestion "Does the scope and DoD above match what you want? (Size M…)". | quotes/paths above |

### Enhancement candidates (max 5 per pair):
- **EC-D6: Auto-derived contract tally** — package test-contract total hand-edited 20→24→26 with KICKOFF.md re-edited twice to stay consistent → gate-check computes the case count from epic tables and stamps CONTRACT/KICKOFF, failing coherence on divergence → epic-authoring/coherence phase.
- **EC-D7: Mandatory packaging/loader test case for plugin work** — the colon-in-YAML-frontmatter bug ("silently strips **all** skill metadata") surfaced only at deploy, after 26/26 green → when the deliverable is a plugin/skill, Test Contract type-selection must include ≥1 install/load/metadata-parse case → test-contract typing rules.
- **EC-D8: Optional "release & comms" epic at scope gate** — deploy, README news, CHANGELOG, LinkedIn post all requested after DoD was met → G1 offers a pre-sized release/announcement epic so these land inside the contract instead of as untracked tail work → scope-gate/DoD phase.
- **EC-D9: Promote fresh-context verification to a v3 invariant** — golden-case judging and final report audit used fresh-context agents with "no access to the implementation conversation", demonstrably keeping claims honest → make an isolated verifier pass a required gate-check step before "shipped" → verification/close-out phase.

## TL;DR
- e2e pair: all 7 epics shipped honestly against frozen CONTRACT v1.0→v1.1; Test Contracts real (12/12 E7, adversarial SYN-1→GAP, `fp9-9.99` rejected, 162+362+184=708 reconciled).
- Biggest e2e failure: cross-subscription handoff — `/read-chat "e2e-unit-tests-plan"` fuzzy-matched the wrong session (`db-organization-plan`); exec patched `distill_session.py` mid-run.
- Pre-grounding trusted a false claim (master-admin "no test script yet" vs actual `"test": "jest"`).
- Env realities (exit-125 port conflicts, broken `test:e2e`, Docker availability flipping 8 INV cases) all discovered in execution, not planning.
- Real bug surfaced by the new QA lane: `graph.service.ts:44-46` missing `@Optional()`.
- fable pair: cleanest observed run — KICKOFF verbatim prompt + CONTRACT v1.1 + 26-case pre-registered contract → v2.0.0, PRs #5–#11, 26/26, zero mid-build interventions.
- Contract sizing churned 20→24→26 by hand; tallies should be derived, not edited.
- Only fable late discovery was packaging-time: unquoted YAML `description:` with colon+space strips skill metadata — no loader test case existed.
- Fernando's recurring ask in BOTH plans: cost-aware model tiers for subagents → should be a default plan artifact.
- Keep: freeze gate + gate-check.mjs, dated contract amendments, evidence ledgers, fresh-context verifiers, single batched decision gate.
