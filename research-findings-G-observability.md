# Findings G: observability-enhance-plan (feedback-obs W1–W5) → 3 execution sessions

## Pair: observability-feedback-plan → observability-feedback-execution + observability-pt3-exec + done-observability-beacon

Session identities (from digests):
- PLAN: `observability-feedback-plan` (session `95f71a69-382a-4d88-b55f-7c7a1b75817e`, initially named `observability-enhance-plan`, renamed at close). Produced epic #59, sub-issues #60–64, package in `beacon/docs/implementation/2-feedback-observability/` (frozen CONTRACT §7 of `02-design-and-contract.md`, `prds/PRD-master.md`, `epics/epics-feedback-obs.md` with 20-case Test Contract, `KICKOFF.md` + copy-paste /fable-it prompt).
- EXEC 1: `observability-feedback-execution` — builds epic #59 (E1 rename canon, E2 four-section nesting, …) against the frozen CONTRACT.
- EXEC 2: `observability-pt3-exec` (session `3faf2520-70e1-43d2-812f-64fc695489f8`) — /fable-it completion of the OLDER observability epic (#8/#9, WS-A/B/C) off `KICKOFF-completion-2026-07-04.md`, a lean plan-it output authored at the tail of session 3.
- EXEC 3: `done-observability-beacon` (session `0552fa46-5062-48b7-b2ef-99dd34cc0960`, 2026-07-02→07-04) — shipped observability P1–P3 to prod; both consumer of a prior handoff note and author of the completion kickoff. (Digest middle hard-truncated by trimmer; findings limited accordingly.)

### Verdict (1 line): **B+** — the full plan-it run's artifacts (frozen contract, 20-case Test Contract, KICKOFF+launch prompt) demonstrably steered execution, but every cross-session handoff carried at least one broken path, stale claim, or unrunnable test precondition that execution had to repair.

### Evidence table

| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | EXEC 1 followed the plan tightly: epics executed in dependency order, tests named per contract cases, board synced. | Exec-1: `backend/tests/test_e1_rename_canon.py`, `libs/anm_llm/tests/test_span_naming.py`, `test_nested_spans.py`, `test_e2_four_section_nesting.py`; "CONTRACT §7" cited; "PR #301 open, #60 In Review" |
| 1 | Plan fidelity (deviation) | EXEC 2 deviated where the lean kickoff mis-specified reality: `scrubber.py` to "reuse" was retired — recovered from git history; KICKOFF path in the goal prompt didn't resolve. | pt3-exec [6]: "The KICKOFF path doesn't exist. Let me find the real doc"; [32]: "`services/scrubber.py` was **retired** with the gold corpus — WS-B (d) can't 'reuse' it" |
| 1 | Plan fidelity (deviation) | EXEC 3's inherited kickoff note was factually wrong about PR scope — following it would have shipped P1+P2+P3 in one prod deploy. | done-obs [25]: "The kickoff note said #282 is 'P1 only, tasks #10/#11/#12/#16.' It is not. **Merging it = shipping P1+P2+P3 to prod in one shot**" |
| 2 | Late discoveries | Plan phase itself CAUGHT big ones early (win): "Generate Plan (PSA) makes zero LLM calls" → dropped from W1; PSA "has *no completion signal at all*"; score-push de-risked via existing `otlp_auth` pk:sk. | plan [40], [58], [signal] "the credential (`otlp_auth` pk:sk pair) already exists; it's IAM wiring, not new infra" |
| 2 | Late discoveries | Execution-side surprises planning should have owned: no pytest locally (hand-rolled `verify_wsb.py`); FE-sentry changed `package.json` without `package-lock.json` → CI failed on PR #299; Sentry v8 `beforeSend`/`ErrorEvent` typing; evals2's prod module importing `tests.eval` (Docker layering vetting needed). | pt3-exec [36] "Python 3.14 available (no pytest…)"; [266] "**#299 (Sentry): `lint-and-typecheck` FAILED**"; [234]–[238] |
| 3 | Test Contract health | Contract well-formed and USED: "A **20-case binding Test Contract** across the 5 epics… DoD = 100% pass… 6 `[REAL]` cases must run against live staging/Langfuse (no fake greens)." EXEC 1 wrote tests per case — BUT `[REAL]` cases deferred: "PENDING: case #5 [REAL] Langfuse names — batched into REAL-verification task"; done-ish claims ran ahead of live verification. | plan [141]/[signal]; exec-1 tail |
| 3 | Test Contract health (untestable case) | EXEC 2 had a case blocked on data no plan task provisioned: "κ≥0.6 **BLOCKED** on ≥25 human labels (no fabrication)"; prod-trace verify similarly BLOCKED on WS-B deploy. Honest stops, but the plan never scheduled the prerequisites. | pt3-exec [signal] table rows 3–5 |
| 4 | Human friction | PLAN session: "You've reached your Fable 5 limit" mid-Gate-G3; user ran `/model` → Opus 4.8 and typed only "continue"; assistant "reading that as **G3 approval**"; also "Task list reset on the model switch." | plan [90]–[97] |
| 4 | Human friction | EXEC 3: user interrupted the ship-scope AskUserQuestion, switched model, redirected to a review-then-harden loop the plan didn't contain ("let's review the code developed by Opus comparing with the spec and prds… use 10 use cases e2e"); later "how can I get the email and password for prod langfuse?"; [131] user noticed leftover board cards → new /plan-it. EXEC 2: merge-path AskUserQuestion (self-approve impossible); "great. let's do these pending things on the order suggested 1, 2 3." | done-obs [7], [122], [131]; pt3-exec [288], [11] |
| 5 | Rework | (a) scrubber recovered from git after kickoff said "reuse" a deleted file; (b) coordinator re-fixed FE-sentry's lockfile + `ErrorEvent` typing after a flagged-but-unresolved subagent risk; (c) EXEC 3 spent its early segment re-verifying branch contents because the kickoff scope claim was false; (d) plan session re-armed a timed-out monitor and drafted a self-run fallback for stalled stream-C. | pt3-exec [32]–[36], [267]–[282]; done-obs [23]–[27]; plan [67]–[72] |
| 6 | Cross-session handoff | Plan→EXEC 1 WORKED: `KICKOFF.md` w/ copy-paste launch prompt, memory `feedback-obs-plan-handoff.md`, board epic #59 — EXEC 1 reconstructed state from KICKOFF + CONTRACT + epics and created its own `.taskstate/feedback-obs-decisions.md` / `feedback-obs-grounding.md` with zero user questions. | plan [130]–[133]; exec-1 taskstate paths `/Users/macbook/Workspace/ANM/Beacon/.taskstate/feedback-obs-*.md` |
| 6 | Cross-session handoff (failures) | (a) EXEC 2's launch prompt cited a KICKOFF path that failed first resolve — the doc exists at TWO levels (`…/Beacon/docs/…` AND `…/Beacon/beacon/docs/…`, both in EXEC 3's files-touched list); (b) "`ghp.sh` isn't at the expected path" (repo vs workspace level) hit BOTH the plan session and EXEC 2; (c) stale memory: "PR #282 is *merged* (the memory saying it needed rebase was stale)"; (d) EXEC 3's context pointer named the wrong session — the `/read-chat "done-github-issues-project"` digest "was actually about **PM tooling**, not the observability build"; (e) EXEC 2 had to disambiguate parallel epics: "A second, separate feedback-obs initiative (epic #59) exists — I'll keep it distinct from my target (epics #8/#9)." | pt3-exec [6]; plan [113]–[115]; plan [39-signal]; done-obs [23]; pt3-exec [18] |
| 7 | Wins to keep | Frozen CONTRACT §7 + Test Contract cited verbatim by EXEC 1's tests/PRs; durable per-slice research files (`research/10-`, `11-`, `12-`, `13-`) made the plan crash-safe ("all research captured… nothing is at risk if a session or agent dies") when the Fable limit killed the session mid-gate; tiered fan-out (haiku stream-A / sonnet C-D / Fable synthesis) delivered; honest VERIFIED / IMPLEMENTED-NOT-VERIFIED / BLOCKED ladder survived into EXEC 2's report; board epic+sub-issues (#59/#60–64, #65–68) served as the live cross-session interface. | plan [49], [141]; pt3-exec [signal] table; exec-1 |

### Enhancement candidates (max 5):
- **EC-G1: Handoff path lint** — problem: launch prompts/kickoffs cited unresolvable paths (pt3-exec [6] "The KICKOFF path doesn't exist"; KICKOFF duplicated at two directory levels; `ghp.sh` path miss in two sessions) → plan-it mechanism: Phase-10 lint executes every path/command in KICKOFF + launch prompt from the target cwd and fails handoff on any miss; forbid duplicate-location doc writes → Phase 10 (handoff lint).
- **EC-G2: Limit/resume-proof gates** — problem: Fable limit hit mid-Gate-G3; after `/model` switch a bare "continue" was inferred as G3 approval and "Task list reset on the model switch" (plan [90]–[97]) → mechanism: statechart persists `gate: PENDING(question-set)`; on resume/model-switch the gate-check hook re-presents the exact AskUserQuestion rather than accepting free text; tasks rehydrate from machine.json → deterministic core (gate-check + hook).
- **EC-G3: Claims audit at pre-ground** — problem: inherited kickoff asserted PR #282 = "P1 only" (actually P1+P2+P3, done-obs [25]); stale memory claimed "#282 needs rebase" (it was merged); wrong `/read-chat` target → mechanism: pre-grounding verifies every PR/branch/issue/file claim in inherited kickoff/memory via one-command checks (`gh pr view --json`, `git log origin/main..`) into a spec-drift table before planning proceeds → Phase 0–1 (pre-grounding).
- **EC-G4: Test Contract runnability + data-prerequisite column** — problem: κ≥0.6 case "BLOCKED on ≥25 human labels" with no labeling task; no pytest in the exec env; `[REAL]` cases batched after "done" claims → mechanism: each contract case declares runner, env prereqs, and data prereqs; human-supplied prereqs auto-generate plan tasks + gates; `[REAL]` cases block epic "done" rather than deferring to a batch → Phase 8 (epics + Test Contracts).
- **EC-G5: Per-workstream integration acceptance checks** — problem: subagent slices passed "done" but failed integration: lockfile-less `package.json` broke CI (#299), prod module imported `tests.eval` needing ad-hoc image vetting → mechanism: plan-it epic template embeds mandatory integration checks (lockfile regen, `tsc --noEmit`, prod-import isolation, `terraform validate`) the executing coordinator runs before accepting any agent slice → Phase 8, feeding /fable-it's verify step.

## TL;DR
- Plan→exec handoff via KICKOFF + frozen CONTRACT + board epic #59 worked: EXEC 1 rebuilt full state from files with zero user questions.
- Every OTHER handoff artifact had a defect: unresolvable KICKOFF path, doc duplicated at two levels, wrong `/read-chat` pointer, stale "needs rebase" memory, `ghp.sh` path miss ×2.
- Worst single failure: kickoff claimed PR #282 was "P1 only" — it bundled P1+P2+P3; merging = surprise full prod deploy; caught only by exec-side git re-audit.
- 20-case Test Contract was real and used, but `[REAL]` live cases were batched after "done", and the κ≥0.6 case was unrunnable without ≥25 human labels never scheduled.
- Usage-limit + model switch mid-Gate-G3 produced an inferred approval from "continue" and a task-list reset — gates aren't resume-proof.
- Env feasibility unchecked: no pytest locally, npm lockfile regen absent from contracts, prod-vs-tests import layering vetted ad hoc.
- Pre-grounding wins cited by execution: PSA zero-LLM scope cut, `otlp_auth` cred de-risk, W3 zero schema change.
- Durable per-slice research files made the plan crash-safe through the mid-gate session death.
- Honest VERIFIED / IMPLEMENTED-NOT-VERIFIED / BLOCKED ladder propagated plan→exec and kept "done" honest at hard human gates.
- Human interventions clustered at: gate re-approval after limit, merge-policy calls, prod credential access, and noticing leftover board cards.
