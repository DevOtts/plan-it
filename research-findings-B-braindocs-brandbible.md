# Findings B: brain-docs-refactor & brand-docs-bible (client-bible-enhance)

## Pair: brain-docs-refactor-plan → brain-docs-refactor-execution + all-to-main-cleanup

### Verdict (1 line): **A-** — execution followed the frozen package near-verbatim with zero mid-run redirects on scope; misses were environmental (plugins absent in the target subscription config, dirty brain-docs repo, stale memory pointer) and git-policy (push scope ambiguity), not spec.

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | High. Kickoff read the package in the plan's prescribed order and ran all 9 workstreams per catalog with the exact per-lane model tiering (WS-1/WS-9 fable, WS-2/3/4/6/7 sonnet, WS-5 haiku). Deviations were flagged in STATUS.md as the CONTRACT required, not renegotiated. Post-DoD the session added a full staleness audit/rewrite pass beyond the 9 WS (scope growth, recorded in STATUS, not thrown away). | USER [8]: "READ FIRST, IN ORDER: 0. /read-chat 'done-brain-docs-refactor-plan' 1. delivery/CONTRACT.md — v1.0 FROZEN … Do NOT renegotiate; flag deviations in delivery/STATUS.md"; ASSISTANT [49] edits STATUS "## Flags raised during execution\n- **W3-KE…"; ASSISTANT [52]-[59] dispatch lanes with "model": "fable"/"sonnet"/"haiku" matching catalog tiering; ASSISTANT [337] records "post-DoD s[taleness audit]" in STATUS.md |
| 2 | Late discoveries | (a) brain-docs working tree had unresolved merge-conflict hunks from parallel work at execution start — plan froze the package without a clean-tree precheck. (b) The 8ftools subscription didn't have the plan-it/fable-it/parallel-lifecycle plugins installed — a whole side-session (all-to-main-cleanup) was spent installing them + restart. (c) Engine-Core main was entangled with 4 unpushed MCP-program commits held "local main only" by another program. | ASSISTANT [36]-[40]: grep -c '<<<<<<<' then "keep HEAD side of each conflict hunk … Merge clean"; all-to-main-cleanup ASSISTANT [23]: "Both were **not installed** in this subscription (8ftools) … ⚠️ Restart required"; ASSISTANT [373]: "There are **7** unpushed commits on Engine-Core main, not 3 … memory explicitly flags that program's origin push as 'user decision — local main only'" |
| 3 | Test Contract health | Docs program: acceptance = CONTRACT §7 greps per workstream, not code tests. Genuinely used and gated: coordinator verified each lane on completion notification, ran repo-wide sweeps before claiming done, and WS-8 acceptance was a live dry-run. No done-claim accepted on lane say-so. | USER [8] DoD: "all 9 workstreams ✅ against CONTRACT §5+§7 … a debrief dry-run promotes a fact + bumps verified: (WS-8 acceptance)"; ASSISTANT [11]/457: "I'll verify each lane against the CONTRACT §7 rubric as completion notifications arrive, escalate any failed acceptance to fable"; ASSISTANT [324]-[325]: "Sweep is clean: zero needs-review, zero stale audit blockquotes" |
| 4 | Human friction | ~3 interventions, all git/close-out, none about the spec: (1) verify debrief skills + push; (2) push-scope correction ("3 local commits" was actually 7 → AskUserQuestion); (3) new-scope request (conclude-it skill). Plus the separate plugin-install session. | USER [40]: "did you update/refacto/enhance the session-debrief and engine-session-debrief as well? After that, push brain-docs and OttVault to main"; USER [41]: "great. push the 3 local commits, delete docs/brain-docs-refactor branch…"; ASSISTANT [374] AskUserQuestion: "git push sends all 7 — I can't push just the 3. How do you want to proceed?" |
| 5 | Rework | Minimal. Conflict-hunk resolution + gitignore fix before WS work; memory pointer rewritten twice because the plan-written pointer went stale ("next session runs KICKOFF"). No workstream output discarded. | ASSISTANT [343]: "Updating the MEMORY.md index pointer (it still says 'next session runs KICKOFF')"; ASSISTANT [340]-[342] rewrite memory/brain-docs-refactor-plan.md twice |
| 6 | Cross-session handoff | Smooth on package content: CONTRACT/catalog/STATUS all found at frozen paths and read first; /read-chat pulled the plan session in via subagent. Rough on environment: plugins missing in ~/.claude-8ftools; the memory pointer was the stale navigation aid despite .plan-it/brain-docs-refactor.state.json existing. | ASSISTANT [5]-[8]: Reads of delivery/CONTRACT.md, delivery/00-workstream-catalog.md, delivery/STATUS.md then distill_session.py "done-brain-docs-refactor-plan"; all-to-main-cleanup ASSISTANT [8]: "DevOtts marketplaces … registered under the **personal** ~/.claude config, but this session runs under **8ftools**" |
| 7 | Wins to keep | (a) The plan's baked-in volatile-fact re-verify ("W3-KEY still unmerged … git fetch origin before concluding") fired and prevented a stale-doc error. (b) Frozen CONTRACT + 9-WS catalog + per-lane tiering executed 1:1. (c) "Flag deviations in STATUS.md, do NOT renegotiate" kept the run honest. (d) READ-FIRST-IN-ORDER kickoff prompt grounded the session in ~8 tool calls. | USER [8]: "RE-VERIFY before writing: W3-KEY still unmerged (don't claim it shipped); ground against LIVE git/services, not the old docs; git fetch origin … (Stream A's own gotcha)"; ASSISTANT [16] runs exactly that check; ASSISTANT [48] flips STATUS checkbox "- [x] W3-K…" |

### Enhancement candidates (max 5 per pair):
- **EC-B1: Execution-environment preflight** — problem: execution machine/subscription wasn't ready — fable-it/plan-it/parallel-lifecycle plugins absent from ~/.claude-8ftools (whole side-session to install + restart), and brain-docs had live merge-conflict hunks at kickoff (ASSISTANT [36]-[40]; all-to-main-cleanup ASSISTANT [23]) → plan-it emits a delivery/PREFLIGHT.md (or extends gate-check.mjs) with runnable checks: required plugins/skills present in the target config dir, every touched repo clean + fetched, expected services up → package-freeze phase (alongside KICKOFF.md).
- **EC-B2: Git policy block in CONTRACT** — problem: push scope was never pinned; user said "push the 3 local commits" but main held 7 entangled with another program's "local main only" hold, forcing an AskUserQuestion (ASSISTANT [373]-[374]) → CONTRACT gets a per-repo git-policy section (working branch, merge strategy, push-when, known entanglements with other in-flight programs) frozen at the decision gate → CONTRACT freeze phase.
- **EC-B3: Statechart-owned memory pointer** — problem: the plan-written memory pointer ("next session runs KICKOFF") went stale and was manually rewritten twice mid/post-execution (ASSISTANT [340]-[345]) → the v2 deterministic core (machine.json/state.json transitions) should rewrite the memory pointer on state change (PLANNED→EXECUTING→SHIPPED) instead of leaving prose pointers to rot → deterministic-core/hook phase.
- **EC-B4: Formalize the "volatile facts" list (generalize a win)** — problem-inverted: the ad-hoc "RE-VERIFY before writing: W3-KEY still unmerged … git fetch origin" line demonstrably prevented a stale claim (USER [8] → ASSISTANT [16]/[48]) but only existed because the user hand-wrote it into the kickoff → plan-it auto-collects every fact that can change between freeze and execution (unmerged branches, service states, env flags) into a KICKOFF "RE-VERIFY FIRST" table with the exact command per fact → KICKOFF authoring phase.

## Pair: brand-docs-bible-plan → brand-bible-execution

### Verdict (1 line): **B** — the 26-case Test Contract + evidence-ledger machinery worked superbly (25/26 VERIFIED, fresh-eyes verifier zero challenges), but the plan's scope ended at local verify: prod deploy exposed a multi-tenant credential-architecture miss (env-var R2 creds vs shared Docker image → per-org Integrations rewire), test-auth was unplanned, and a plan-time-known gap (INV-2 no regression test) was frozen in anyway.

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | High through build+verify: waves, epics E1-E5, tiering ("coordinator Fable; haiku for mechanical, sonnet for spec'd-against-tests, fable for E3 storage core + adversarial verify") and file-scope/no-co-editing rules all executed as frozen. One delivered-UI deviation: T-E4-05 assumed a per-row visibility toggle; shipped UI sets visibility at upload time — logged as "DEVIATION (noted, non-blocking)". Fidelity broke at the deploy phase, which the package didn't cover. | USER [4] kickoff quoting the package: "CONTRACT.md v1.0 … DoD = 100% of each epic's Test Contract passes (epics/epics-client-bible-enhance.md, 26 cases)"; ASSISTANT [211]: "the doc row has **only** a 'Delete document' button — no per-row visibility toggle. The delivered UI sets visibility at **upload time**"; ASSISTANT [230] evidence entry "DEVIATION (noted, non-bloc…" |
| 2 | Late discoveries | (a) Multi-tenant blast radius: "The 3 repos build ONE shared Docker image per repo across tenants" — un-flag-gated E1/E2 UI reaches every tenant; Mongoose hydrates missing approved field to default — settled only during deploy investigation. (b) Credential architecture wrong: shipped module reads R2 creds from env vars; correct home is per-org Integrations records → full rewire design needed. (c) Local test auth unknown: passwords unknowable, bootstrap-admin 409s on non-empty users, forced a direct bcrypt hash reset (admin@usengine.io → FableIt2026!). (d) Mongo on 27027 not 27017; .taskstate/ root polluted by a prior June run (had to namespace client-bible-run/). (e) T-E5-04 blocked by a pre-existing broken env ("openai.BadRequestError: invalid model ID" in brain-agent). (f) Parallel mcp-tparty squash-merge made CB branches stale. | ASSISTANT [271] AskUserQuestion: "The 3 repos build ONE shared Docker image per repo across tenants … the approval banner + set-default UI are NOT flag-gated"; ASSISTANT [275]: "The shipped implementation reads R2 credentials from **env vars** (R2_ACCESS_KEY_ID etc. via ConfigSer…"; ASSISTANT [69]-[75]: "Local passwords are unknown … bootstrap-admin only works on an empty users collection (409 otherwise)"; ASSISTANT [33]: "there's an unrelated decisions.md from a June run I must not clobber"; ASSISTANT [229]: "pre-existing and environmental — openai.BadRequestError: invalid model ID"; ASSISTANT [261]: "my Client Bible branches are now **stale/diverged** because the MCP work squash-merged into origin/main" |
| 3 | Test Contract health | Excellent and load-bearing. 26 typed cases ([REAL]/E2E/unit) drove the run; the [REAL] tag triggered a pre-build live R2 smoke that converted G-OPS-1/2 from assumed to proven; done-claims required same-session ledger entries; fresh-eyes verifier (fable, ledger-only, no code access) confirmed all 26 rows, zero challenges; T-E5-04 honestly held at IMPLEMENTED-NOT-VERIFIED. Gaps: T-E4-05 phrased against widget mechanics that didn't ship (deviation), T-E5-04 untestable due to pre-existing env, no prod-deploy cases at all, and INV-2 lacked a regression test — known at plan time ("Handoff lint green. One judgment-half gap from the consistency pass: **INV-2** (new orgs get the default bible attached) has no regression test", plan digest). | ASSISTANT [63]: "**Live R2 smoke test passed end-to-end** against the real praxya bucket … both G-OPS-1 and G-OPS-2 are satisfied — the [REAL] cases T-E3-06 and T-E4-05 are genuinely verifiable, not INV"; evidence.md rule (ASSISTANT [36]): "A DoD criterion may be reported VERIFIED **only** …"; ASSISTANT [246]: "The fresh-eyes verifier **CONFIRMED all 26 rows and the tally** — zero challenges"; final: "25 of 26 Test-Contract cases VERIFIED; 1 (T-E5-04) honestly IMPLEMENTED-NOT-VERIFIED" |
| 4 | Human friction | 4 interventions: (1) user had to ask in the kickoff for the credentials list ("give me here which credentials and access you need from Cloudflare…"); (2) creds pointer + CDP: "1 and 2. Cloudflare credentials add at line 53 praxya_tenant.cloudflare_r2 in @.secrets/.full.credentials / 3. CDP open!"; (3) mid-run scope add: "ps.: I also add two brand-guideline files … 'what is the pallet colors of ANM?' … Can I leave?"; (4) deploy kickoff + parallel-conflict warning: "let's continue the implementation, deploy it and test in prod. Just be aware of the last deploys we did in another parallel session, to avoid conflicts: /read-chat 'mcp-tparty-execution'"; plus the tenant-rollout AskUserQuestion whose answer "reframes the architecture significantly". Items 1, 4 and the reframe were plan-pinnable. | USER [4], USER [7], USER [12], USER [23], ASSISTANT [271]-[272] |
| 5 | Rework | Largest of both pairs: the entire document-storage credential path must be rewired from env-var ConfigService to per-org encrypted Integrations records + on-demand S3Client (dedicated Plan-agent design + new plan file quiet-munching-quail.md), because the plan never modeled the shared-image/per-tenant deployment reality. Smaller: JWT/password-reset scaffolding, .taskstate re-namespacing, r2-smoke script relocation for ESM resolution. | ASSISTANT [276]: "(read-only + produce a plan) the rewire of the brain-api-core document-storage module so it sources Cloudflare R2 credentials PER-ORG fr[om Integrations]"; ASSISTANT [278] Write /Users/macbook/.claude-8ftools/plans/quiet-munching-quail.md: "was built and verified (25/26) but is **uncommitted**" |
| 6 | Cross-session handoff | Package loaded cleanly — delivery tree found, CONTRACT/program-plan/epics/KICKOFF read first, and "Everything in the contract checks out against the real code: the target files exist at the cited paths". Two context compactions mid-run were survived via auto-summaries. Friction: coordinator's own first repo-path guess was wrong ("The repos weren't at the path I guessed"), shared .taskstate/ collided with a prior run, and cross-parallel-session state (mcp-tparty deploys) had to be imported manually by the user via /read-chat. | ASSISTANT [2]-[7] reads; ASSISTANT [17]: "the target files exist at the cited paths"; ASSISTANT [9]: "The repos weren't at the path I guessed"; USER [13]/[22] compaction summaries; USER [23] parallel-session warning |
| 7 | Wins to keep | (a) 26-case typed Test Contract as the DoD — execution treated it as the verdict, not agent claims. (b) [REAL] case tagging → early live smoke de-risked the crown jewel before any code. (c) Ops-gates G-OPS-1/2 named in the plan let execution prove them satisfied. (d) Evidence ledger + fresh-eyes verifier (ledger-only) — zero-challenge audit. (e) Interface lock in decisions.md frozen "from CONTRACT.md v1.0 + source-of-truth reads" before fan-out. (f) Tiering executed as written (E3 on top tier, UI/backend on sonnet). | USER [4]: "Ops-gated: R2 bucket + public domain per Space (G-OPS-1/2) before storage live-verify"; ASSISTANT [35]: "Frozen from CONTRACT.md v1.0 + source-of-truth reads. Interface lock: suba…"; ASSISTANT [50]: "dispatching three parallel builders (E3 storage on opus — the crown jewel; E1+E2 backend and UI on sonnet)" |

### Enhancement candidates (max 5 per pair):
- **EC-B5: Rollout/deployment design as a first-class plan artifact** — problem: the frozen package ended at local verification; deploy revealed shared-Docker-image-per-repo multi-tenancy, un-flag-gated UI blast radius, and a wrong credential home, forcing a mid-deploy architecture reframe and a full storage-module rewire (ASSISTANT [271]-[276], quiet-munching-quail.md) → plan-it adds a rollout section to the design docs (tenancy model, image/flag topology, credential residency, blast-radius per epic) and puts tenant-facing rollout questions in the ONE batched decision gate → design-doc + decision-gate phases.
- **EC-B6: Test-auth & credentials audit at plan time** — problem: execution had to ask for creds in chat, then discover auth was un-mintable (unknown passwords, bootstrap-admin 409) and reset admin@usengine.io to FableIt2026! via direct Mongo write (ASSISTANT [68]-[75], USER [7]) → plan-it pre-generates delivery/CREDENTIALS-NEEDED.md (creds, secrets locations, a verified test-login path per app) during pre-grounding so the human can provision before kickoff instead of mid-run → pre-grounding/discovery phase.
- **EC-B7: Known-gap freeze blocker** — problem: the plan's own handoff lint flagged "INV-2 (new orgs get the default bible attached) has no regression test" yet the package froze without resolving or logging it as binding debt (plan digest, handoff-lint step) → gate-check.mjs refuses freeze while lint gaps are open, or forces them into a CONTRACT "accepted debt" table with an owner → package-freeze/gate-check phase.
- **EC-B8: Parallel-program interlock in CONTRACT** — problem: mcp-tparty's squash-merge to origin/main silently staled the CB branches; only the user's manual warning ("Just be aware of the last deploys we did in another parallel session … /read-chat 'mcp-tparty-execution'") surfaced it (USER [23], ASSISTANT [261]) → plan-it's pre-grounding enumerates other in-flight programs touching the same repos (scan .plan-it/*.state.json + memory pointers) and writes a rebase/sync gate into CONTRACT → pre-grounding + CONTRACT phases.
- **EC-B9: Outcome-phrased E2E cases** — problem: T-E4-05 encoded a widget mechanic (per-row visibility toggle) the build legitimately didn't ship, producing an avoidable DEVIATION entry (ASSISTANT [209]-[211], [230]) → Test-Contract authoring rule: E2E cases assert observable outcomes ("doc becomes public and its R2 URL resolves 200"), never specific UI affordances, unless the affordance is itself a requirement → epic/Test-Contract authoring phase.

## TL;DR
- Pair 1 grade A-: 9/9 workstreams shipped per frozen CONTRACT with prescribed model tiering, zero spec renegotiation; deviations flagged in STATUS.md as designed.
- Pair 2 grade B: build+verify excellent (25/26 VERIFIED, fresh-eyes verifier zero challenges) but prod deploy exposed a plan-scope hole.
- Biggest single miss (pair 2): multi-tenant deployment model absent from plan — env-var R2 creds vs one-shared-image-per-repo → full per-org Integrations rewire (rework).
- Environment readiness was never planned: plugins missing in the 8ftools config dir (pair 1), unknown test-auth/passwords + wrong Mongo port (pair 2).
- Git policy gaps caused the only pair-1 friction: push scope ("3 commits" were 7, entangled with another program's local-only hold) and pre-existing conflict hunks at kickoff.
- Plan-time-known gap shipped: handoff lint flagged INV-2 had no regression test; package froze anyway.
- Parallel sessions are a real hazard: mcp-tparty squash-merge staled CB branches; user had to manually warn via /read-chat.
- Test Contract machinery is the crown jewel to keep: typed cases, [REAL] tags forcing early live smokes, evidence ledger with VERIFIED-only-with-evidence rule, fresh-eyes ledger-only verifier.
- The hand-written "RE-VERIFY before writing (W3-KEY)" volatile-facts instruction prevented a stale claim — should be auto-generated by plan-it, not hand-authored.
- Memory pointers written by plan-it rot ("next session runs KICKOFF" survived past shipping twice); tie pointer updates to state.json transitions.
