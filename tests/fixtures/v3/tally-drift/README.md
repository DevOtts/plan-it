# Fixture: tally-drift

Consumed by: **C-W5-01** (`contract` verb).
Violates: "Hand-typed \"N/N\" tally disagreeing with computed `@case-` count → gate FAILS."

Contents:
- `delivery/v3/CONTRACT.md` — hand-typed `Tally: 5/5` line; the table actually
  has 2 `@case-` tagged rows (F-CASE-01, F-CASE-02).
