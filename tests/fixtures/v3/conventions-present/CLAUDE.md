# Fixture repo — conventions-present

<!-- plan-it:test-conventions -->
Run tests with `npm test`. Unit tests live in `tests/`. No special fixtures
required for CI.
<!-- /plan-it:test-conventions -->
