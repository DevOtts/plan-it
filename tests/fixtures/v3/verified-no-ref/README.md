# Fixture: verified-no-ref

Consumed by: **C-W4-02** (`handoff` verb's VERIFIED-reference check).
Violates: "`VERIFIED` tag lacking a run-output reference → `handoff` FAILS."

Contents:
- `delivery/notes.md` — a bare `VERIFIED` with no run-output citation nearby.
