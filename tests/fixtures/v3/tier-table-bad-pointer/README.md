# Fixture: tier-table-bad-pointer

Consumed by: **T-B2-02** (Tier Table 4-field parser — negative case).
Violates: `scaffold-pointer` cell is inline prompt prose with spaces, not a
`fable-it:<id>` / `.claude/agents/<name>.md` path.
