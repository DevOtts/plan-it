# Fixture: credentials-gated

Consumed by: **T-B3-07** (credential gate, EC-B7/E5).
Satisfies (positive case): credential row `status: "GATED-with-owner"` with
an `owner` field, and the same row appears verbatim in
`delivery/v3/CONTRACT.md` → accepted.
