# Fixture: unreviewed

Consumed by: **C-W1-03** (`freeze` verb, `--dir` extension).
Violates: "`freeze` verb FAILS while `casesReviewed !== true` in state.json."

Contents:
- `.plan-it/state.json` — `casesReviewed: false`.
- `delivery/v3/CONTRACT.md` — minimal contract copy.
