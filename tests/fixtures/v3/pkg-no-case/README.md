# Fixture: pkg-no-case

Consumed by: **C-W1-04** (`contract` verb).
Violates: "`contract` verb FAILS when plan diff touches `plugin.json`/
`SKILL.md`/`marketplace.json` and zero `@case-packaging` rows exist."

Contents:
- `DIFF-MANIFEST.txt` — names `plugin.json`/`SKILL.md` as touched.
- `delivery/v3/CONTRACT.md` — minimal contract copy with zero `@case-packaging` rows.
