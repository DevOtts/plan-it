# Fixture: gaps-dispositioned

Consumed by: **T-B3-05** (freezeGate known-gaps ledger check, EC-B7).
Satisfies (positive case): every `knownGaps` row has a
`disposition ∈ {fix, waive, case-ify}` → `G3_APPROVED` accepted.
