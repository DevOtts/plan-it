# Fixture: gaps-undispositioned

Consumed by: **T-B3-04** (freezeGate known-gaps ledger check, EC-B7).
Violates: `gates.G3.knownGaps` has one row (`GAP-1`) with no `disposition`
field → `G3_APPROVED` must be rejected.
