# Fixture: state-replan-mode.json

Consumed by: **T-B3-03** (Phase-0 machine selector).
Satisfies (positive case): `machine: "machine-replan.json"` → the selector
must resolve to the replan machine instead of the default `machine.json`.
