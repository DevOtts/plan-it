# Fixture: pluginlint-name-mismatch

Consumed by: **T-C2-04** (`pluginlint` verb).
Violates: "frontmatter YAML parses but skill name ≠ directory name → fail."
`SKILL.md` declares `name: foo` inside a directory named `bar/`.
