# Fixture: tier-table-good

Consumed by: **T-B2-02** (Tier Table 4-field parser — positive case).
Satisfies: all 4 fields present; `scaffold-pointer` is a `build-it:<id>`
path, not inline prompt text.
