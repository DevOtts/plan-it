# Fixture: pluginlint-bad-source

Consumed by: **T-C2-03** (`pluginlint` verb).
Violates: "marketplace.json plugins[] `source` path that doesn't exist on
disk → fail." `./plugins/does-not-exist` is not present in this fixture.
