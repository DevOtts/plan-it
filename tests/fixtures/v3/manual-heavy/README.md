# Fixture: manual-heavy

Consumed by: **C-W1-06** (`contract` verb).
Violates: "`manual:` share >30% → warning + non-zero exit unless `--override-manual`."

Contents:
- `delivery/v3/CONTRACT.md` — 2 of 3 case rows are `manual:` (~67%, over the 30% gate).
