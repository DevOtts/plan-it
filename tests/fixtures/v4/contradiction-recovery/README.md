# Fixture: contradiction-recovery

Consumed by: **T-V4B2-11** / **C-E7-06** (`tests/v4/core/mode-consistency.mjs`).
Positive fixture: history carries `freeze —REVIEW_CONTRADICTED→
parallelPlanning` with one default (`D1`) contradicted once and matched in
`gates.G4.contradictions[]` — accepted; the recovery edge (`AMENDMENT`) is
listed among next events.
