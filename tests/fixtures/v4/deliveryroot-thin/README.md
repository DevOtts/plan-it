# Fixture: deliveryroot-thin

Consumed by: **T-V4B3-06** / **C-E9-03** (`deliveryroot-resolution.mjs`).
A thin (adversary-shallow) CONTRACT under `delivery/v4/CONTRACT.md` —
`adversary --dir <fx> --run v4` and the positional `adversary
<fx>/delivery/v4` must agree, both naming the D-B3 cascade gaps, proving
the v4 contract was actually read (same shape as `tests/fixtures/v3/
adversary-thin`).
