# Fixture: disposition-contract-case

Consumed by: **T-V4B4-12** / **C-E8-02** (`node .../gate-check.mjs reconcile
--dir tests/fixtures/v4/disposition-contract-case`). `## Residuals` row
`Item: T-B2-04` carries `backlog-with-reason: …` — a contract case never
moves to backlog (G-9) — exit 1 naming `T-B2-04` and `C-E8-02`.
