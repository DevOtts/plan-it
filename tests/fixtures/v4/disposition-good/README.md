# Fixture: disposition-good

Consumed by: **T-V4B4-14** / **C-E8-01** (`tests/v4/core/disposition-good.mjs`).
Positive fixture: two `VERIFIED` rows (empty and `—` disposition), one
`owner-gated:`, one `IMPLEMENTED-NOT-VERIFIED:`, one `backlog-with-reason:`
whose path (`backlog/x.md`) exists on disk, typed tally equal to computed —
`reconcile --dir` exits 0.
