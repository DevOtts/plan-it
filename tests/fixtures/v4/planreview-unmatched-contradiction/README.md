# Fixture: planreview-unmatched-contradiction

Consumed by: **T-V4B2-08** / **C-E7-03** (`tests/v4/core/mode-consistency.mjs`).
Violates: a default `contradicted:true` (id `D2`) with no matching entry in
`gates.G4.contradictions[]` — REJECTED naming the id.
