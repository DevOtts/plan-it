# Fixture: mirror-fresh

Consumed by: **T-V4B4-01** / **C-E2-06** (`tests/v4/core/mirror-stale.mjs`).
Positive fixture: `X.html`'s `planit-source` stamp hash equals `X.md`'s
current SHA-256 — `mirror X.md X.html` exits 0, `MIRROR_FRESH`.
