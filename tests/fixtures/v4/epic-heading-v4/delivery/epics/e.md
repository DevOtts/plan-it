## V4B1 — sample

A bare V4-shaped epic heading with no "Epic" word and no Tier Table — the
3.0.1 heading grammar ([A-Z]\d+) can't see it at all (heading invisible,
reconcile exits 0); the widened v4 grammar ([A-Z]{1,3}\d+[A-Z]?\d*) does,
so reconcile fails naming C-W3-01 and V4B1.
