# Fixture: disposition-tally-drift

Consumed by: **T-V4B4-13** / **C-E8-03** (`node .../gate-check.mjs reconcile
--dir tests/fixtures/v4/disposition-tally-drift`). Typed `Dispositions: 3
backlog · 1 owner-gated · 1 INV` disagrees with the computed `2/1/1` — exit
1 printing both tallies and `C-E8-03` (G-5). A `## Log` bullet tagged
`[incidental]` mentions case IDs but changes no count.
