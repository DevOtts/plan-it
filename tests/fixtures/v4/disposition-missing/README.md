# Fixture: disposition-missing

Consumed by: **T-V4B4-11** / **C-E8-01** (`node .../gate-check.mjs reconcile
--dir tests/fixtures/v4/disposition-missing`). STATUS board has a
`Disposition` column; row `F1` is `IN-PROGRESS` with an empty cell — exit 1
naming `F1` and `C-E8-01` (G-3).
