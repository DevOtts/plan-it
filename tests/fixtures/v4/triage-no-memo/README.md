# Fixture: triage-no-memo

Consumed by: **T-V4B2-02** / **C-E6-01** (`state --dir tests/fixtures/v4/triage-no-memo --run t1`).
Violates: verdict `"skip"` with `triage.memo` naming a file that does not
exist on disk — REJECTED as `CLOSED_WITHOUT_PLAN REJECTED`, fix named (write
the memo).
