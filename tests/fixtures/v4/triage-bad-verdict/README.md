# Fixture: triage-bad-verdict

Consumed by: **T-V4B2-01** / **C-E6-01** (`state --dir … --run t2`).
Violates: `triage.verdict` is `"defer"`, outside the closed set
`plan | build-instead | owner-decision | skip` — REJECTED naming the four
allowed verdicts.
