# Fixture: disposition-bad-path

Consumed by: **T-V4B4-14** / **C-E8-01** (`tests/v4/core/disposition-good.mjs`).
`backlog-with-reason: delivery/backlog/does-not-exist.md` — the named path
does not exist on disk — exit 1 naming the missing path.
