# Fixture: draft-no-defaults

Consumed by: **T-V4B2-14** / **C-E7-05** (`tests/v4/core/freeze-draft.mjs`,
`freeze --draft --dir tests/fixtures/v4/draft-no-defaults`).
Violates: same draft header as `draft-header/`, but `gates.G2.defaults` is
`[]` — REJECTED ("nothing defaulted, nothing to review").
