# PRD — fixture (AMD-7 / T-V4B4-17)

## Rationale

This run applied several defaults from its own recommendations (R2, R10)
without asking the human — see `gates.G2.defaults` for the full list. It
also has one genuinely orphaned requirement, R99, mentioned nowhere else.

## Requirements

- R99: a real requirement that no epic covers (orphan, must be flagged)
