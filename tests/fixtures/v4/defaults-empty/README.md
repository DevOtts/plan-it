# Fixture: defaults-empty

Consumed by: **T-V4B2-04** / **C-E7-07** (`state tests/fixtures/v4/defaults-empty/.plan-it/v4.state.json`).
Violates: `defaultsApplied` is in history but `gates.G2.defaults` is `[]` —
REJECTED naming C-E7-07.
