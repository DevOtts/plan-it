# Fixture: guard-unfrozen-named-frozen

Consumed by: **T-V4B3-14** / **C-E9-08** (`tests/v4/core/guard-unfrozen-named.mjs`).
The recovery twin of `guard-unfrozen-named/`: the same named run
(`v4.state.json`, `run.deliveryRoot: "delivery/v4/"`) with
`contract.version` recorded (`"1.0-draft"`) — the write the unfrozen twin
denies is ALLOWED here, both shipped guard copies.
