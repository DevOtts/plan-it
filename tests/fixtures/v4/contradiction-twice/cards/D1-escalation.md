# ESCALATED — default D1 (topology)

Contradicted twice (solo → orchestrator+squads → solo again). Per governance
this default is never re-applied automatically; the owner decides directly.

Status: open — awaiting owner decision.
