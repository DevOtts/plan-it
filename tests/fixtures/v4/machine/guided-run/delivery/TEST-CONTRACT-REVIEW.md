# Test Contract review — fixture stub

Reviewed-by: Fernando Ott 2026-09-07

This file exists only to satisfy the FD-2 guided-mode B1/B2 check
(`tests/fixtures/v4/machine/guided-run/` is a T-V4B1-06 / T-V4B2 machine
fixture — its `gates.G2.approved` is `true` because `decisionGate` is in
history, so `state` requires this artifact on disk).
