# Findings A: airtable-database + anm-process

## Pair: done-airtable-database-plan → airtable-database-execution

### Verdict (1 line): **A-** — execution consumed the package verbatim (CONTRACT cited, canonical test IDs driven, KICKOFF Phase-0 honored), but infra ground truth (RabbitMQ/Redis) was wrong until the user corrected it, and the contract needed a v1.3 build-time amendment.

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | High. Execution launched via `/fable-it` from the KICKOFF prompt, read the frozen package, and built exactly the planned surface: `brain-api-core/src/integrations/airtable/` (client, cache, settings, module, errors, specs) + admin-ui settings page. Contract amendments were recorded, not silently ignored. | Exec files touched: `/Users/macbook/Workspace/Loudr/brain-api-core/src/integrations/airtable/airtable-client.service.ts`, `airtable-cache.service.spec.ts`, `src/integrations/airtable/airtable-settings.service.spec.ts`, `/settings/airtable/page.tsx`; exec edits `delivery/CONTRACT.md` with versions `v1.1`, `v1.2`, `v1.3` visible |
| 2 | Late discoveries | (a) **Infra facts wrong in plan**: research agents called RabbitMQ "dead code" and Redis "net-new"; user corrected: "we do have rabbitMQ and Redis in loudragency VPS tenant 5.161.60.199:31739" (plan USER [22]). Plan re-verified: "`amqplib` IS a dependency and brain-api-core has a *full* RabbitMQ manager" → CONTRACT v1.2 amendment A6. (b) Execution still had to resolve concrete Redis connectivity (non-standard port `6399` appears 8x, `REDIS_URL` x2, `REDIS_CLIENT` x3 in exec) and env plumbing (`AIRTABLE_V1_BASE_ID`, `RUN_AIRTABLE_REAL`). | plan ASSISTANT [217–222]: "my earlier 'RabbitMQ is dead code, ignore it' framing was too dismissive… Folding in a CONTRACT v1.2 amendment"; exec identifiers `6399` x8, `REDIS_URL`, `RUN_AIRTABLE_REAL` x2 |
| 3 | Test Contract health | Used and load-bearing. 144 cases / 30 `[REAL]` declared; exec drives canonical IDs directly (`E1-01`, `E1-03`, `E1-06`, `E2-01`–`E2-11`, `E9-01`–`E9-10` visible in exec transcript), `[REAL]` cases gated behind `RUN_AIRTABLE_REAL=1` against live base `app2TwrsHfsW5i7qd`. But the plan's own lint caught squad defects first: epics-3 "declared 12/12/10 but 9/9/8 rows", root cause "`[REAL]` tag sits *inside* the ID cell (`\| T-E9-02 [REAL] \|`)" — squad-declared counts were not trustworthy until the deterministic check ran. | plan ASSISTANT [193–198]: "Handoff lint **passes** — 144 distinct test IDs, all count checks green"; exec spec files `airtable-client.real.spec.ts` x2 |
| 4 | Human friction | 1 substantive intervention in PLAN: the RabbitMQ/Redis infra correction (USER [22], with screenshot). Planning research never probed the deployed VPS, only the repos. Exec-side interventions not fully legible in digest; at least one AskUserQuestion on UI verification method occurred. | plan USER [22]: "we do have rabbitMQ and Redis in loudragency VPS tenant 5.161.60.199:31739 [Image #5]" |
| 5 | Rework | Moderate, all inside planning: (a) all 12 epic files + 4 PRDs rewritten with perl to convert squad IDs `T-AT-apicore-access-layer-01`-style → canonical `T-E<N>-NN` because "my squad prompts gave a descriptive `T-<EID>-NN` convention instead"; (b) `[REAL]`-tag cell fix in epics-3/epics-4; (c) `prds/`+`epics/` relocated "under `delivery/` (the canonical layout the guard scans)"; (d) post-freeze doc sweep to fix RabbitMQ/Redis wording across findings/vision/README/KICKOFF/memory. | plan ASSISTANT [183–196] |
| 6 | Cross-session handoff | Worked. Exec located and read `delivery/CONTRACT.md`, `delivery/epics/epics-1-apicore.md`, `delivery/00-program-plan.md`, and the plan's memory file `memory/airtable-database-plan.md` (x3 in exec) with no path hunting visible. Execution added `delivery/WAVE-0-ground-truth.md` (x3) — the env-verification wave the KICKOFF pre-seeded ("makes them Phase 0 rather than a question back to you"). | exec files touched: `/Users/macbook/Workspace/Loudr/brain-docs/implementations/airtable-database/delivery/WAVE-0-ground-truth.md` x3 |
| 7 | Wins to keep | (a) Deterministic `gate-check.mjs` handoff lint — caught real count/ID defects pre-handoff; (b) frozen CONTRACT with versioned amendment protocol — execution extended it to v1.3 instead of drifting; (c) KICKOFF with inlined launch prompt + Phase-0 env checks; (d) `[REAL]`-tagged live-API cases; (e) memory + vault pointer let exec load context instantly; (f) "idle ≠ delivered" verify rule (Rule 3) applied to all fan-outs. | plan ASSISTANT [198], [216]; exec `WAVE-0-ground-truth.md`, `CONTRACT.md` `v1.3` |

### Enhancement candidates (max 5):
- **EC-A1: Infra ground-truth probe** — problem: plan grounded 4 repos but mislabeled live infra (RabbitMQ "dead code", Redis "net-new"); user had to correct with the VPS address (USER [22]), forcing CONTRACT v1.2 + a 6-file doc sweep → proposed mechanism: a mandatory "deployment reality" discovery stream (docker/VPS services, connection strings, ports, env endpoints) alongside code streams, outputting an ENV-FACTS sheet with *verified* values (exec still burned time on Redis port `6399`) → phase: pre-grounding / discovery fan-out.
- **EC-A2: Lint grammar inside squad prompts** — problem: squads invented `T-<EID>-NN` IDs and put `[REAL]` inside the ID cell; conductor perl-rewrote 16 files → proposed mechanism: squad prompt template embeds the exact `T-E<N>-NN` grammar + table format, and each squad must run `gate-check.mjs handoff` on its own files before reporting done → phase: parallelPlanning.
- **EC-A3: Canonical layout given up front** — problem: `prds/`+`epics/` were authored outside `delivery/` and relocated after the lint failed ("the canonical layout the guard scans") → proposed mechanism: machine.json declares the target tree; squads receive absolute output paths; gate-check validates location on first run → phase: backboneFreeze (folder scaffold emitted at freeze).
- **EC-A4: Execution-side amendment protocol as a first-class artifact** — problem: execution had to amend CONTRACT to v1.3 ad hoc; the amendment channel exists only by convention → proposed mechanism: plan-it ships an AMENDMENTS section + rule ("build-time fact corrections go here, architecture changes need re-gate") in CONTRACT.md and records expected state in `.plan-it/state.json` so a later `plan-it --audit` can diff plan vs as-built → phase: handoff.

## Pair: anm-process-plan → anm-process-execution

### Verdict (1 line): **B-** — the package was coherent and rich, but the DoD churned twice *after* freeze on user corrections (video DoD, tool reuse), the pasted launch prompt arrived corrupted in the exec session, and that exec session built **nothing** (read docs, stopped, user pivoted to `/shopify-forge`).

### Evidence table
| # | rubric item | finding | evidence quote/path |
|---|---|---|---|
| 1 | Plan fidelity | Untestable in this exec session — zero execution occurred. Exec read KICKOFF/CONTRACT/00-program-plan/STATUS/README then: "Stopped. I've read the KICKOFF, CONTRACT, program plan, STATUS, and README, but I haven't built anything or launched any lanes yet — nothing was written or changed." User then invoked `/shopify-forge` and interrupted the AskUserQuestion. | exec ASSISTANT [9]; exec USER [7] `/shopify-forge`; USER [9] "[Request interrupted by user for tool use]" |
| 2 | Late discoveries | Found in PLAN, post-"complete": (a) `notebooklm` CLI declared "NOT installed" by the plan's PATH probe, but the user corrected — "both tools already exist on the 8FA side, so I should **reuse, not reinstall**" — verified at `/Users/macbook/Workspace/8FA/brain-agent/venv/bin/notebooklm` v0.5.0, auth `status: ok` + `token_fetch: true`; (b) extraction gaps found only mid-plan: Session 01 AMs VTT "extracted to 1 char — different VTT format", HTML prototypes "JS-rendered (only 57–978 chars of static text)"; (c) macOS has no `timeout` binary (auth check false-failed). | plan ASSISTANT [169–170], [190], [204–207], [35–38] |
| 3 | Test Contract health | Written (~200+ cases across 4 PRDs + 24 epics, e.g. squad-A "Test cases: 73 total (A1=15, A2=15, A3=15, A4=16, A5=12), each Setup/Expected/Pass; DoD=100%") but **never exercised** — no build ran. Format variance already caused a false alarm at plan-time: "The `test-rows≈0` for epics-B" because "epics-B uses table-format Test Contracts (`\| # \| Type \| Setup \| Expected \| Pass \|`)" — counted by ad-hoc grep, no deterministic gate-check.mjs equivalent for this non-code package. | plan ASSISTANT [135–137]; squad-a teammate message |
| 4 | Human friction | 2 interventions in PLAN after the package was declared complete: (i) USER [26]: "Let's only enhance our DoD for what we need to build. Let's also use the @…/notebooklm/SKILL.md … and @…/notebooklm-video-builder/SKILL.md" → CONTRACT v1.1 (video DoD = produced MP4, not just script); (ii) reuse-not-reinstall correction → CONTRACT v1.2 + 4-spot sweep of program-plan/KICKOFF ("4 spots that still said 'create venv / notebooklm login / DEFERRED'"). Plus the exec-session pivot itself. | plan ASSISTANT [172], [190], [198–202] |
| 5 | Rework | (a) Post-freeze contract churn v1.0→v1.1→v1.2 plus program-plan + KICKOFF + config rewrites for the video pipeline; (b) failed Edit due to `notebookllm/` vs `notebooklm` "single-l typo" requiring re-read + redo; (c) KICKOFF launch prompt fully replaced once more by `/next-session-prompt` ("Replacing the launch prompt with the refined read→play-back→build→stop version"). | plan ASSISTANT [177], [193–202], [213–214] |
| 6 | Cross-session handoff | Package files loaded fine (exec read all 5 delivery docs by absolute path), but the **copy-paste prompt mechanism failed**: the pasted `/fable-it` args are mojibake-corrupted ("CONTRACT Â§3", "W1 pages+diagrams+SWOT+questionnaire â W2", "status â  failed") and truncated mid-sentence ("the master current+to-ontinuing"), and the user pasted trailing narration into the command ("Artifacts written… - Let's not publish to youtube the video…"). The session then produced no artifacts. | exec USER [5] command-args, lines 37–79 of exec digest |
| 7 | Wins to keep | (a) Corpus normalization to `_build/_corpus/` with `_MANIFEST.tsv` + near-empty-extraction audit ("All 69 files extracted, zero failures" then char-count flagging); (b) SHARED-CONTEXT.md as coherence law for 12 parallel discovery agents (P00–P11); (c) file-existence watcher instead of trusting idle notifications ("Per plan-it's Rule 3 — *idle ≠ delivered*"); (d) prereq reality-check before handoff ("so the handoff is honest about what's installed vs. what you'll need to do"); (e) §3/§10 cross-edge reconciliation invariant referenced by all epics ("all 24 canonical §3 edges are referenced across the epic set"). | plan ASSISTANT [32–34], [40], [62], [135], [169] |

### Enhancement candidates (max 5):
- **EC-A5: Skill/tooling inventory at pre-grounding** — problem: plan probed `command -v notebooklm` on PATH, concluded "NOT installed", and wrote install-from-scratch prereqs into CONTRACT/KICKOFF; user corrected to reuse the 8FA venv install (v0.5.0, already authed) → proposed mechanism: pre-grounding scans known workspaces/venvs/skills for tools the deliverables need (an "available capabilities" probe, like the codebase probe but for tooling) before writing prereqs → phase: pre-grounding.
- **EC-A6: Deliverable-form question in the batched gate** — problem: G2 never asked "is the deliverable the *script* or the *produced MP4*?"; user enhanced the DoD after freeze, causing v1.1/v1.2 churn across CONTRACT/program-plan/KICKOFF/config → proposed mechanism: the decision gate must include one question per deliverable pinning its terminal artifact form + acceptance evidence → phase: decision gate (G2).
- **EC-A7: File-based launch, not pasted-blob launch** — problem: the KICKOFF copy-paste prompt reached the exec session encoding-corrupted (`Â§3`, `â`) and truncated ("current+to-ontinuing"), with stray narration pasted in → proposed mechanism: the canonical launch is a one-liner (`/fable-it` + KICKOFF.md path); KICKOFF.md on disk is the source of truth and the exec session must diff any pasted text against it and prefer the file → phase: handoff.
- **EC-A8: Deterministic handoff lint for non-code packages** — problem: the ANM run had no `gate-check.mjs`-equivalent; test-case counts were verified by ad-hoc grep and epics-B's variant table format produced "test-rows≈0" noise → proposed mechanism: a package-type-aware lint (one enforced Test Contract table schema, count reconciliation, §3-edge coverage check) shipped for doc/knowledge packages too → phase: verify.
- **EC-A9: Execution-liveness follow-through** — problem: the plan ended "ready for /fable-it" but the only exec session stalled at reading and was abandoned; nothing tracks that a frozen package was never built → proposed mechanism: `.plan-it/state.json` gains a `handedOffAt`/`buildStartedAt` field the exec session stamps; a stale unbuilt package surfaces in memory/STATUS → phase: handoff / post-handoff.

## TL;DR
- Airtable pair: execution consumed the package faithfully — CONTRACT cited and amended to `v1.3`, canonical test IDs (`E1-01`…`E2-11`, `E9-*`) driven, `WAVE-0-ground-truth.md` created per KICKOFF's Phase-0.
- Biggest airtable gap: plan verified code, not deployed infra — RabbitMQ/Redis facts wrong until Fernando supplied `5.161.60.199:31739`; exec still had to derive Redis port `6399` and env vars.
- plan-it's deterministic `gate-check.mjs` lint demonstrably worked: caught wrong test-ID grammar, `[REAL]`-in-ID-cell miscounts, and wrong folder layout before handoff — but only because the conductor ran it; squads should self-lint.
- ANM pair: the exec session built **nothing** — it read the 5 delivery docs, stopped, and the user pivoted to `/shopify-forge`; plan fidelity/Test Contracts are unexercised.
- ANM handoff prompt arrived mojibake-corrupted and truncated (`Â§3`, "current+to-ontinuing") — pasted-blob launch is fragile; the file-based KICKOFF should be the only launch vector.
- ANM DoD churned twice after freeze (video = MP4 not script; reuse 8FA notebooklm instead of install) — both were knowable at gate time via a deliverable-form question + a tooling inventory probe.
- Squad self-declared counts were unreliable in both runs (144-case run had 2 miscounting files; ANM epics-B counted ≈0 by grep) — deterministic, format-locked Test Contract tables needed for all package types.
- Wins to generalize: SHARED-CONTEXT.md for fan-outs, "idle ≠ delivered" file watchers, versioned CONTRACT amendments, corpus manifest with empty-extraction audit, prereq reality checks before handoff.
- 9 enhancement candidates total: EC-A1–A4 (infra probe, squad self-lint, canonical layout scaffold, execution amendment protocol) and EC-A5–A9 (tooling inventory, deliverable-form gate question, file-based launch, non-code lint, build-liveness tracking).
